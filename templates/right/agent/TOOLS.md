# Tools and Environment

Declarative notes about how to use your tools — facts and rules, not
commands to yourself. Keep entries compact; this file is part of your
system prompt on every turn.

## Tool selection

<!--
Rules for which tool to prefer when several can do the job.
Example:
- For interactive browser sessions (logins, forms, multi-step flows)
  use browser-use. For simple fetches of public pages use WebFetch.
- For cron / scheduled tasks use the /rightcron skill.
-->

## Environment

<!--
Paths, proxies, installed services, network quirks.
Example:
- Home directory is /sandbox.
- HTTPS_PROXY=http://10.200.0.1:3128 is set inside the sandbox.
-->

## Integrations

<!--
External services you talk to and their gotchas.
Example:
- API foo expects `arguments`, not `input`, for tool calls.
- Service bar rejects requests without a User-Agent header.
-->
