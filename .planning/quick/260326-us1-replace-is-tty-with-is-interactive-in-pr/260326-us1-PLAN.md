---
phase: 260326-us1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/process-compose.yaml.j2
  - .planning/PROJECT.md
  - .planning/milestones/v2.2-ROADMAP.md
  - .planning/SESSION_REPORT.md
autonomous: true
requirements: [US1]

must_haves:
  truths:
    - "process-compose.yaml.j2 uses is_interactive: true instead of is_tty: true"
    - "Known-limitation notes referencing the is_tty restart bug are updated to reflect the change"
  artifacts:
    - path: "templates/process-compose.yaml.j2"
      provides: "process-compose template"
      contains: "is_interactive: true"
  key_links:
    - from: "templates/process-compose.yaml.j2"
      to: "generated process-compose.yaml"
      via: "minijinja render in cmd_up"
      pattern: "is_interactive"
---

<objective>
Replace `is_tty: true` with `is_interactive: true` in the process-compose template and update all planning documents that reference the old field or the associated restart bug.

Purpose: `is_interactive` is the correct process-compose field name (as of v1.x). Using the right field may fix the `rightclaw restart` crash that was previously attributed to `is_tty`.

Output: Updated template + updated planning docs with accurate field name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update template field</name>
  <files>templates/process-compose.yaml.j2</files>
  <action>
    Replace `is_tty: true` with `is_interactive: true` on line 10.
    No other changes to the template — keep the comment on line 1 and all other fields exactly as-is.
  </action>
  <verify>
    <automated>rg "is_interactive: true" /home/wb/dev/rightclaw/templates/process-compose.yaml.j2 && ! rg "is_tty" /home/wb/dev/rightclaw/templates/process-compose.yaml.j2</automated>
  </verify>
  <done>`is_interactive: true` present, `is_tty` absent from template.</done>
</task>

<task type="auto">
  <name>Task 2: Update planning docs</name>
  <files>
    .planning/PROJECT.md
    .planning/milestones/v2.2-ROADMAP.md
    .planning/SESSION_REPORT.md
  </files>
  <action>
    In all three files, replace references to the `is_tty` restart bug as follows:

    1. `.planning/PROJECT.md` line 143:
       Change: `- \`rightclaw restart\` disabled (process-compose is_tty bug)`
       To:     `- \`rightclaw restart\` status unknown — changed \`is_tty\` to \`is_interactive\` (correct field name); restart may now work`

    2. `.planning/milestones/v2.2-ROADMAP.md` line 124:
       Change: `- \`rightclaw restart\` disabled (process-compose is_tty bug)`
       To:     `- \`rightclaw restart\` disabled (process-compose is_tty bug) — field renamed to is_interactive in template; restart status unverified`

    3. `.planning/SESSION_REPORT.md` lines 40-41 block and line 126:
       - Line 40: update `is_tty` note to mention `is_interactive` is the correct field
       - Line 126: change "is_tty fakes enough for CC but breaks restart" to note the field was renamed to `is_interactive`

    Do NOT rewrite surrounding content. Surgical line-level edits only.
  </action>
  <verify>
    <automated>rg "is_interactive" /home/wb/dev/rightclaw/.planning/PROJECT.md /home/wb/dev/rightclaw/.planning/milestones/v2.2-ROADMAP.md /home/wb/dev/rightclaw/.planning/SESSION_REPORT.md</automated>
  </verify>
  <done>All three planning files reference `is_interactive` instead of treating `is_tty` as the accepted workaround.</done>
</task>

</tasks>

<verification>
rg "is_tty" /home/wb/dev/rightclaw/templates/process-compose.yaml.j2 && echo "FAIL: is_tty still in template" || echo "OK: template clean"
rg "is_interactive" /home/wb/dev/rightclaw/templates/process-compose.yaml.j2
</verification>

<success_criteria>
- `templates/process-compose.yaml.j2` contains `is_interactive: true`, no `is_tty`
- Planning docs updated to reflect the field rename (not treat is_tty restart crash as permanent known limitation)
- No Rust code changes required (template is the only source of truth for generated config)
</success_criteria>

<output>
After completion, create `.planning/quick/260326-us1-replace-is-tty-with-is-interactive-in-pr/260326-us1-SUMMARY.md`
</output>
