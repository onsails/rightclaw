# Quick Task 260405-uz6: Show stdio MCP servers in /mcp list and protect rightclaw from /mcp remove

## Tasks

### Task 1: Include stdio servers in mcp_auth_status and protect rightmemory from removal

**Files:** `crates/rightclaw/src/mcp/mod.rs`, `crates/rightclaw/src/mcp/detect.rs`, `crates/bot/src/telegram/handler.rs`, `crates/rightclaw-cli/src/main.rs`

**Action:**
1. Add `PROTECTED_MCP_SERVER` constant to `mcp/mod.rs`
2. Add `ServerKind` enum (Http/Stdio) to `detect.rs`, add `kind` field to `ServerStatus`
3. Include stdio servers in `mcp_auth_status()` instead of skipping them
4. Update bot `/mcp list` to display stdio servers as `-- stdio`
5. Add guard to bot `/mcp remove` rejecting removal of protected server
6. Update CLI `mcp status` to display stdio servers
7. Update tests: rename `stdio_server_skipped` → `stdio_server_included`, add `mixed_http_and_stdio_both_listed`

**Verify:** `cargo build --workspace && cargo test --workspace -- mcp && cargo clippy --workspace`
**Done:** All tests pass, clippy clean
