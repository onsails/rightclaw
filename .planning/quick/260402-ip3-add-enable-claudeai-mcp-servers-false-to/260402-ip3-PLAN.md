---
phase: quick
plan: 260402-ip3
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/process-compose.yaml.j2
  - crates/rightclaw/src/codegen/process_compose_tests.rs
  - .planning/seeds/SEED-015-mcp-nonblocking-env-var.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Generated process-compose.yaml contains ENABLE_CLAUDEAI_MCP_SERVERS=false"
    - "Generated process-compose.yaml contains MCP_CONNECTION_NONBLOCKING=1"
    - "Tests assert both env vars are present in codegen output"
  artifacts:
    - path: "templates/process-compose.yaml.j2"
      provides: "Jinja2 template with both MCP env vars"
      contains: "ENABLE_CLAUDEAI_MCP_SERVERS=false"
    - path: "crates/rightclaw/src/codegen/process_compose_tests.rs"
      provides: "Regression tests for both env vars"
  key_links:
    - from: "templates/process-compose.yaml.j2"
      to: "crates/rightclaw/src/codegen/process_compose.rs"
      via: "include_str! macro embeds template at compile time"
      pattern: "include_str!"
---

<objective>
Add two CC env vars to the generated process-compose.yaml to eliminate startup delays:
- `ENABLE_CLAUDEAI_MCP_SERVERS=false` — prevents loading the user's cloud/account-level MCP servers (Figma, Blockscout, etc.), eliminating ~1.5s blocking delay
- `MCP_CONNECTION_NONBLOCKING=1` — CC starts immediately, connects to MCP servers in background instead of waiting (SEED-015)

Purpose: Agents should be responsive from first second. Both vars are CC-documented, zero risk, one-line additions each.
Output: Updated template + regression tests. SEED-015 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@templates/process-compose.yaml.j2
@crates/rightclaw/src/codegen/process_compose_tests.rs
@.planning/seeds/SEED-015-mcp-nonblocking-env-var.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MCP env vars to template and update tests</name>
  <files>
    templates/process-compose.yaml.j2,
    crates/rightclaw/src/codegen/process_compose_tests.rs
  </files>
  <behavior>
    - Test: generated output contains "ENABLE_CLAUDEAI_MCP_SERVERS=false"
    - Test: generated output contains "MCP_CONNECTION_NONBLOCKING=1"
    - Both tests use make_bot_agent("myagent", "123:tok") — same helper as existing env tests
  </behavior>
  <action>
    1. Write the two failing tests in process_compose_tests.rs (same pattern as existing env var tests):
       - `env_contains_enable_claudeai_mcp_servers_false`
       - `env_contains_mcp_connection_nonblocking`

    2. Run tests to confirm they fail (SEED-015 note: verify var names haven't been renamed in newer CC).

    3. Edit templates/process-compose.yaml.j2 — add both lines to the environment block after the existing RC_* vars and before the closing of the block:
       ```yaml
             - ENABLE_CLAUDEAI_MCP_SERVERS=false
             - MCP_CONNECTION_NONBLOCKING=1
       ```
       Indentation: 6 spaces (matches existing env lines in the template).

    4. Run tests to confirm they pass.

    5. Mark SEED-015 status as `resolved` in .planning/seeds/SEED-015-mcp-nonblocking-env-var.md.
  </action>
  <verify>
    <automated>cargo test -p rightclaw env_contains --no-fail-fast 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both new tests pass. Template contains both env var lines. All existing process_compose_tests pass.
    cargo test -p rightclaw with no failures.
  </done>
</task>

</tasks>

<verification>
cargo test -p rightclaw 2>&1 | tail -20
</verification>

<success_criteria>
- templates/process-compose.yaml.j2 contains ENABLE_CLAUDEAI_MCP_SERVERS=false and MCP_CONNECTION_NONBLOCKING=1
- Two new tests added and passing
- All pre-existing tests still pass
- SEED-015 marked resolved
</success_criteria>

<output>
After completion, create `.planning/quick/260402-ip3-add-enable-claudeai-mcp-servers-false-to/260402-ip3-SUMMARY.md`
</output>
