# Requirements Archive: v2.5 RightCron Reliability

**Archived:** 2026-03-31
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: v2.5 RightCron Reliability

## Milestone Goal

Make rightcron's cron reconciler actually work — fix the bootstrap so it can create the reconciler job, and redesign the skill so reconciliation is deterministic regardless of context.

## Requirements

### Bootstrap Fix

- [x] **BOOT-01**: `rightclaw up` results in a `*/5 * * * *` reconciler cron job existing in the agent session (CronList confirms it) — Phase 21, Plan 01
- [x] **BOOT-02**: `startup_prompt` does not delegate to a background Agent tool — rightcron runs inline in the main thread — Phase 21, Plan 01

### Reconciler Redesign

- [x] **RECON-01**: rightcron skill separates reconciler into CHECK (read-only, outputs structured diff — no CronCreate/CronDelete calls) and RECONCILE (direct CronCreate/CronDelete in main thread based on diff) — Phase 21, Plan 02
- [x] **RECON-02**: After cron fires, jobs defined in `crons/*.yaml` are created/updated/deleted correctly without any Agent tool delegation — Phase 21, Plan 02

### Verification

- ~~**VER-01**: Manual end-to-end test~~ — CANCELLED: Phase 22 skipped, superseded by new milestone approach

## Future Requirements (carried forward)

- Automated test for cron reconciler cycle (deferred — requires live CC session)
- Multi-agent cron isolation (deferred — all agents share session-scoped crons today)

## Traceability

| Requirement | Phase | Plan | Status |
|-------------|-------|------|--------|
| BOOT-01 | Phase 21 | 21-01 | ✅ Complete |
| BOOT-02 | Phase 21 | 21-01 | ✅ Complete |
| RECON-01 | Phase 21 | 21-02 | ✅ Complete |
| RECON-02 | Phase 21 | 21-02 | ✅ Complete |
| VER-01 | Phase 22 | — | ❌ Cancelled |
