---
phase: 19-home-isolation-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - .planning/phases/19-home-isolation-hardening/19-HUMAN-UAT.md
autonomous: false
requirements:
  - HOME-06

must_haves:
  truths:
    - "UAT document exists with 7 test cases covering fresh-init through doctor"
    - "UAT covers Telegram-positive and Telegram-negative agent scenarios"
    - "UAT covers RC_AGENT_NAME provenance in memory round-trip"
  artifacts:
    - path: ".planning/phases/19-home-isolation-hardening/19-HUMAN-UAT.md"
      provides: "Manual test checklist for fresh-init end-to-end validation"
      contains: "rightclaw init"
  key_links: []
---

<objective>
Write comprehensive UAT document for fresh-init validation and get human sign-off.

Purpose: Phases 16-18 built the memory system. Phase 19 Plan 01 fixes HOME isolation bugs. This plan validates the entire v2.3 stack end-to-end from a clean state to catch any remaining integration issues before shipping.

Output: 19-HUMAN-UAT.md with 7 test cases, then human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-home-isolation-hardening/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write UAT document with 7 test cases</name>
  <files>.planning/phases/19-home-isolation-hardening/19-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/19-home-isolation-hardening/19-CONTEXT.md,
    .planning/phases/19-home-isolation-hardening/19-01-SUMMARY.md
  </read_first>
  <action>
    Per D-07, create `.planning/phases/19-home-isolation-hardening/19-HUMAN-UAT.md` with the following 7 test cases. Use markdown checklist format with clear pass/fail criteria.

    The UAT document must contain these sections:

    **Header:**
    ```
    # Phase 19: HOME Isolation Hardening - Human UAT
    **Date:** 2026-03-27
    **Tester:** (fill in)
    **Build:** (commit hash of Plan 01 completion)
    **Prerequisites:** rightclaw binary built from latest, process-compose installed, sqlite3 in PATH
    ```

    **Test 1: Fresh Init**
    - Command: `rm -rf ~/.rightclaw && rightclaw init`
    - Expected: `~/.rightclaw/` directory created, `~/.rightclaw/agents/right/` exists with `IDENTITY.md` and `agent.yaml`
    - Pass criteria: `ls ~/.rightclaw/agents/right/IDENTITY.md` exits 0

    **Test 2: rightclaw up — file generation**
    - Command: `rightclaw up` (then Ctrl+C after agents start)
    - Expected for each agent: `memory.db`, `.claude/settings.json`, `.claude.json`, `.mcp.json`, credential symlinks all exist in agent dir
    - Pass criteria: `ls ~/.rightclaw/agents/right/memory.db ~/.rightclaw/agents/right/.claude/settings.json ~/.rightclaw/agents/right/.claude.json ~/.rightclaw/agents/right/.mcp.json` all exist

    **Test 3: .mcp.json content validation**
    - Command: `cat ~/.rightclaw/agents/right/.mcp.json | python3 -m json.tool`
    - Expected: `mcpServers.rightmemory.env.RC_AGENT_NAME` equals the agent name (e.g., `"right"`); NO `"telegram": true` key at root level
    - Pass criteria: JSON has `RC_AGENT_NAME` key with value matching agent name; `"telegram"` key absent from root

    **Test 4: Agent WITHOUT telegram — no channels injection**
    - Prerequisite: Default agent has no `telegram_token` or `telegram_token_file` in `agent.yaml`
    - Check wrapper: `cat /tmp/rightclaw-run/*/right-wrapper.sh` (or wherever wrappers are generated)
    - Expected: wrapper does NOT contain `--channels`
    - Check settings: `cat ~/.rightclaw/agents/right/.claude/settings.json`
    - Expected: settings.json does NOT contain `enabledPlugins`
    - Pass criteria: grep for `--channels` in wrapper returns empty; grep for `enabledPlugins` in settings.json returns empty

    **Test 5: Agent WITH telegram — channels injected correctly**
    - Setup: Add `telegram_token: "TEST:faketoken"` to an agent's `agent.yaml`, run `rightclaw up`
    - Expected: wrapper contains `--channels plugin:telegram@claude-plugins-official`; `.claude/channels/telegram/.env` contains `TELEGRAM_BOT_TOKEN=TEST:faketoken`
    - Pass criteria: grep for `--channels` in wrapper returns match; `.env` file contains token

    **Test 6: Memory round-trip with correct provenance**
    - Setup: Start agent session, use MCP tool to store a memory (e.g., via agent prompt)
    - Command: `rightclaw memory list <agent-name>`
    - Expected: Entry shows `stored_by` equal to the agent name (NOT "unknown")
    - Pass criteria: `stored_by` column value matches agent name

    **Test 7: rightclaw doctor — all checks pass**
    - Command: `rightclaw doctor`
    - Expected: All checks pass (bubblewrap, socat, sqlite3, git)
    - Pass criteria: Exit code 0, no FAIL lines in output

    **Results table** at the bottom:
    ```
    | Test | Status | Notes |
    |------|--------|-------|
    | 1. Fresh Init | [ ] Pass / [ ] Fail | |
    | 2. File Generation | [ ] Pass / [ ] Fail | |
    | 3. .mcp.json Content | [ ] Pass / [ ] Fail | |
    | 4. No Telegram | [ ] Pass / [ ] Fail | |
    | 5. With Telegram | [ ] Pass / [ ] Fail | |
    | 6. Memory Round-trip | [ ] Pass / [ ] Fail | |
    | 7. Doctor | [ ] Pass / [ ] Fail | |
    ```
  </action>
  <verify>
    <automated>test -f /home/wb/dev/rightclaw/.planning/phases/19-home-isolation-hardening/19-HUMAN-UAT.md && echo "UAT file exists"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-home-isolation-hardening/19-HUMAN-UAT.md` exists
    - File contains `# Phase 19: HOME Isolation Hardening - Human UAT`
    - File contains 7 numbered test sections (Test 1 through Test 7)
    - File contains `rightclaw init` in Test 1
    - File contains `memory.db` in Test 2
    - File contains `RC_AGENT_NAME` in Test 3
    - File contains `--channels` in Test 4
    - File contains `telegram_token` in Test 5
    - File contains `stored_by` in Test 6
    - File contains `rightclaw doctor` in Test 7
    - File contains a results table with 7 rows
  </acceptance_criteria>
  <done>UAT document created with 7 comprehensive test cases and results table</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human runs UAT and reports results</name>
  <what-built>
    Phase 19 Plan 01 fixed three HOME isolation bugs:
    1. Telegram false-positive — agents without telegram config no longer get --channels/enabledPlugins
    2. RC_AGENT_NAME injection — .mcp.json now passes agent name to memory server
    3. mcp_config_path field removal — dead code cleaned up

    The UAT document covers end-to-end validation from fresh init to doctor checks.
  </what-built>
  <how-to-verify>
    1. Build rightclaw: `cargo build --workspace` in the project root
    2. Open `.planning/phases/19-home-isolation-hardening/19-HUMAN-UAT.md`
    3. Execute each of the 7 tests in order
    4. Record pass/fail in the results table
    5. Report any failures with details
  </how-to-verify>
  <resume-signal>Type "all pass" or describe which tests failed and what you observed</resume-signal>
</task>

</tasks>

<verification>
1. UAT document exists at `.planning/phases/19-home-isolation-hardening/19-HUMAN-UAT.md`
2. Human has executed all 7 test cases
3. All test cases pass (or failures are triaged)
</verification>

<success_criteria>
- UAT document written with all 7 test cases from D-07
- Human has verified the fixes work end-to-end
- All v2.3 capabilities validated from a clean state
</success_criteria>

<output>
After completion, create `.planning/phases/19-home-isolation-hardening/19-02-SUMMARY.md`
</output>
