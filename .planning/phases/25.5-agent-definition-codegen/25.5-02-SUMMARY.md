---
phase: 25.5-agent-definition-codegen
plan: "02"
subsystem: bot
tags: [telegram, claude-code, structured-output, json-schema, worker]

requires:
  - phase: 25.5-01
    provides: agent_def.rs codegen writing reply-schema.json and agents/<name>.md to agent_dir/.claude/

provides:
  - worker.rs uses --agent <name> on first CC call (AGDEF-02)
  - worker.rs uses --json-schema <path> on every CC call (AGDEF-03, D-01)
  - parse_reply_output replaces parse_reply_tool: direct serde deserialization of result object (D-03, AGDEF-04)
  - REPLY_TOOL_JSON const deleted; --append-system-prompt and --system-prompt-file removed
  - WorkerContext carries agent_name extracted from agent_dir basename

affects: [phase-26-process-compose-wiring, phase-27-cron-runtime, integration-testing]

tech-stack:
  added: []
  patterns:
    - "CC structured output: --output-format json + --json-schema produces {session_id, result: {...}} -- deserialize result directly via serde"
    - "agent_name extracted from agent_dir.file_name() at WorkerContext construction in handler.rs"

key-files:
  created: []
  modified:
    - crates/bot/src/telegram/worker.rs
    - crates/bot/src/telegram/handler.rs

key-decisions:
  - "--agent only on first call (AGDEF-02); resume inherits agent from CC session (AGDEF-03)"
  - "--json-schema on both first and resume calls per D-01 (Pitfall 4 avoidance)"
  - "ReplyOutput gains #[derive(Debug, Deserialize)] -- reused struct, no new type needed"
  - "agent_name = agent_dir.file_name() -- directory basename is the canonical agent name"

patterns-established:
  - "parse_reply_output: parse serde_json::Value, extract session_id, deserialize result field directly via serde_json::from_value"

requirements-completed: [AGDEF-02, AGDEF-03, AGDEF-04]

duration: 3min
completed: 2026-04-01
---

# Phase 25.5 Plan 02: Agent Definition Codegen Summary

**worker.rs migrated from --system-prompt-file + tool_use search to --agent/--json-schema with direct serde deserialization of CC structured output result object**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-01T13:31:48Z
- **Completed:** 2026-04-01T13:34:53Z
- **Tasks:** 1 (TDD: RED commit + GREEN commit)
- **Files modified:** 2

## Accomplishments

- Deleted REPLY_TOOL_JSON const, parse_reply_tool, find_reply_tool_input, --append-system-prompt, --system-prompt-file from worker.rs
- Added parse_reply_output: deserializes `result` object field directly via serde (D-03)
- invoke_cc uses --agent <name> on first call, --json-schema on both calls (AGDEF-02/03)
- WorkerContext gains agent_name field; handler.rs extracts it from agent_dir basename
- 14 bot unit tests pass; workspace builds clean

## Task Commits

1. **Test (RED): add failing tests for parse_reply_output** - `b008974` (test)
2. **Task 1: migrate worker to --agent/--json-schema** - `81e218d` (feat)

**Plan metadata:** (docs commit follows)

_TDD: RED commit (failing tests) → GREEN commit (implementation + passing tests)_

## Files Created/Modified

- `crates/bot/src/telegram/worker.rs` - Replaced parse_reply_tool with parse_reply_output; rewrote invoke_cc command construction; deleted REPLY_TOOL_JSON and find_reply_tool_input; added agent_name to WorkerContext; added Debug+Deserialize derives to ReplyOutput
- `crates/bot/src/telegram/handler.rs` - Extract agent_name from agent_dir.file_name() and populate WorkerContext.agent_name

## Decisions Made

- `--agent` omitted on resume calls because CC session already knows the agent; adding it would conflict (AGDEF-03)
- `--json-schema` on both calls per D-01 to ensure structured output on every response (Pitfall 4)
- `ReplyOutput` reused with added `#[derive(Debug, Deserialize)]` -- no new struct needed, existing fields match schema
- `agent_name` extracted at WorkerContext construction time in handler.rs (not stored in DB) -- ephemeral, always from current agent_dir

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added Debug derive to ReplyOutput**
- **Found during:** Task 1 (GREEN phase, compile error)
- **Issue:** `unwrap_err()` in test requires `Debug` bound; plan specified only `Deserialize`
- **Fix:** Added `Debug` to `#[derive(Debug, serde::Deserialize)]`
- **Files modified:** crates/bot/src/telegram/worker.rs
- **Verification:** Tests compile and pass
- **Committed in:** 81e218d (Task 1 commit)

**2. [Rule 1 - Bug] Fixed media_paths test assertion type mismatch**
- **Found during:** Task 1 (GREEN phase, compile error)
- **Issue:** `Some(["a.png"].as_slice())` produces `Option<&[&str]>` but field is `Option<Vec<String>>`
- **Fix:** Changed to `let paths = output.media_paths.unwrap(); assert_eq!(paths, vec!["a.png".to_string()])`
- **Files modified:** crates/bot/src/telegram/worker.rs
- **Verification:** Test compiles and passes
- **Committed in:** 81e218d (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 missing critical, 1 bug)
**Impact on plan:** Both trivial compile-time fixes. No scope creep.

## Issues Encountered

None significant.

## Known Stubs

- `media_paths` in ReplyOutput: field is parsed and logged as warning but not yet sent as Telegram media (carried from Phase 25, tracked in original plan).

## Next Phase Readiness

- Phase 25.5 complete: agent_def.rs codegen (Plan 01) + worker.rs migration (Plan 02)
- Phase 26 (process-compose wiring) can proceed: bot processes now expect agent_dir/.claude/agents/<name>.md and reply-schema.json
- No blockers

---
*Phase: 25.5-agent-definition-codegen*
*Completed: 2026-04-01*
