# Phase 25.5: Agent Definition Codegen - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-01
**Phase:** 25.5-agent-definition-codegen
**Areas discussed:** --json-schema arg format, tools: whitelist in frontmatter, AGENTS.md in agent body

---

## --json-schema arg format

### How does CC's --json-schema accept the schema?

| Option | Description | Selected |
|--------|-------------|----------|
| File path | rightclaw up writes .claude/reply-schema.json; worker passes path | ✓ |
| Inline JSON string | Schema as const in worker.rs, passed directly | |
| Don't know — research | Researcher checks CC docs before planning | |

**User's choice:** File path
**Notes:** Schema persists on disk, easy to inspect/debug in production.

---

### Where does CC put the structured data with --json-schema?

| Option | Description | Selected |
|--------|-------------|----------|
| result key directly (object) | {"session_id":"...","result":{"content":"...",...}} | ✓ |
| Same result array, different type | Structured data as JSON string inside text block | |
| Don't know — research | Researcher checks CC docs first | |

**User's choice:** At result key directly (object)
**Notes:** parse_reply_tool replaced with simple deserialization of result field.

---

## tools: whitelist in frontmatter

| Option | Description | Selected |
|--------|-------------|----------|
| Omit tools: entirely | No tools: field; CC uses defaults; sandbox is security layer | ✓ |
| Hardcode standard set | Always emit [Bash, Read, Write, Edit, ...] | |
| Derive from sandbox config | Map excluded_commands blocklist to CC tool restrictions | |

**User's choice:** Omit tools: entirely
**Notes:** OS sandbox (bubblewrap/Seatbelt) is the enforcement layer, not CC tool restriction.

---

## AGENTS.md in agent body

| Option | Description | Selected |
|--------|-------------|----------|
| Leave as-is | AGENTS.md unchanged; --json-schema enforces output natively | ✓ |
| Add structured output guidance | Append JSON schema contract section to template | |
| Ship new AGENTS.md via rightclaw up | Regenerate from template on every up (risks overwriting) | |

**User's choice:** Leave as-is
**Notes:** --json-schema enforces structure mechanically; tool-call instruction not needed in agent body.

---

## Claude's Discretion

- Whether `generate_reply_schema()` is a `&'static str` const or a separate write function
- Exact serde struct for `result` deserialization (reuse existing `ReplyOutput` struct)
- Test coverage approach for the new JSON parsing path

