---
phase: 03.1-claude-code-settings-plugin-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rightclaw/src/init.rs
autonomous: true
requirements:
  - CHAN-01
  - CHAN-02

must_haves:
  truths:
    - "When Telegram token is provided, .claude/settings.json is created in the agent directory with enabledPlugins for Telegram"
    - "When no Telegram token is provided, .claude/settings.json is NOT created"
    - "No manual /plugin install step is needed — the settings file auto-enables the plugin"
  artifacts:
    - path: "crates/rightclaw/src/init.rs"
      provides: ".claude/settings.json creation logic"
      contains: "enabledPlugins"
  key_links:
    - from: "crates/rightclaw/src/init.rs"
      to: "agents/right/.claude/settings.json"
      via: "std::fs::write inside telegram_token.is_some() branch"
      pattern: "settings\\.json"
---

<objective>
Write `.claude/settings.json` with `enabledPlugins` for Telegram during `rightclaw init` when a Telegram token is provided.

Purpose: Eliminates the manual `/plugin install telegram@claude-plugins-official` step so agents launch with Telegram plugin auto-enabled.
Output: Modified `init.rs` with settings.json creation + tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crates/rightclaw/src/init.rs
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from init.rs. -->

From crates/rightclaw/src/init.rs:
```rust
pub fn init_rightclaw_home(
    home: &Path,
    telegram_token: Option<&str>,
    telegram_env_dir: Option<&Path>,
) -> miette::Result<()>
```

The function already:
1. Creates `agents/right/` directory with template files (lines 39-61)
2. Installs built-in skills (lines 63-75)
3. Writes Telegram `.env` file when token provided (lines 77-97)
4. Prints summary and "Next: run /plugin install..." hint (lines 99-112)

The `.claude/settings.json` write should go after the `.env` write (line 97) and the println hint on line 111 should be replaced.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add .claude/settings.json creation to init_rightclaw_home</name>
  <files>crates/rightclaw/src/init.rs</files>
  <read_first>crates/rightclaw/src/init.rs</read_first>
  <behavior>
    - Test: init with telegram token creates `.claude/settings.json` in agent dir containing `{"enabledPlugins":{"telegram@claude-plugins-official":true}}` (per D-02)
    - Test: init without telegram token does NOT create `.claude/settings.json` in agent dir (per D-03)
    - Test: existing `init_creates_default_agent_files` test still passes (no regression)
  </behavior>
  <action>
    Modify `init_rightclaw_home` in `crates/rightclaw/src/init.rs`:

    1. **Add settings.json write** (per D-01, D-02): Inside the `if let Some(token) = telegram_token` block (after the `.env` write at line 97), add:
       - Create `.claude/` directory inside `agents_dir`: `agents_dir.join(".claude")`
       - Write `settings.json` with content: `{"enabledPlugins":{"telegram@claude-plugins-official":true}}`
       - Use `serde_json::json!` macro for clean JSON construction, or a raw string literal (discretion: raw string is simpler, no new dependency needed since this is a fixed format)

    2. **Do NOT create .claude/settings.json when no token** (per D-03): The write is inside the existing `if let Some(token)` block, so this is automatic.

    3. **Update println output** (line 108-112): Replace the "Next: run /plugin install..." hint with a line like `"  agents/right/.claude/settings.json (Telegram plugin enabled)"`. Remove the instruction to manually run /plugin install since it is no longer needed.

    4. **Add two tests** in the `#[cfg(test)] mod tests` block:
       - `init_with_telegram_creates_settings_json`: Call with token, assert `.claude/settings.json` exists in agent dir, assert content contains `enabledPlugins` and `telegram@claude-plugins-official`
       - `init_without_telegram_no_settings_json`: Call without token, assert `.claude/settings.json` does NOT exist in agent dir

    5. **Build workspace**: Run `cargo build --workspace` and `cargo clippy --workspace` to verify.

    Implementation note: Use a raw JSON string (`r#"{"enabledPlugins":...}"#`) rather than adding serde_json dependency — this is a fixed, known format with no dynamic fields.
  </action>
  <verify>
    <automated>cd /home/wb/dev/rightclaw && cargo test -p rightclaw -- init::tests --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - rg "enabledPlugins" crates/rightclaw/src/init.rs returns matches
    - rg "settings\\.json" crates/rightclaw/src/init.rs returns matches
    - rg "plugin install" crates/rightclaw/src/init.rs returns NO matches (old hint removed)
    - cargo test -p rightclaw -- init::tests passes all tests including new ones
    - cargo build --workspace succeeds
  </acceptance_criteria>
  <done>
    init_rightclaw_home creates .claude/settings.json with enabledPlugins when Telegram token provided, skips it when no token, old /plugin install hint removed, all tests pass, workspace builds clean.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p rightclaw -- init::tests` — all tests pass (including two new settings.json tests)
- `cargo build --workspace` — clean build
- `cargo clippy --workspace` — no warnings
- `rg "enabledPlugins" crates/rightclaw/src/init.rs` — confirms settings content
- `rg "plugin install" crates/rightclaw/src/init.rs` — no matches (old hint gone)
</verification>

<success_criteria>
When `rightclaw init` is called with a Telegram token, `.claude/settings.json` is created in the agent directory with the Telegram plugin enabled. When called without a token, no settings file is created. The manual `/plugin install` instruction is removed from output.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-claude-code-settings-plugin-configuration/03.1-01-SUMMARY.md`
</output>
