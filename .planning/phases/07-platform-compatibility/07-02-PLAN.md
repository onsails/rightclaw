---
phase: 07-platform-compatibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
autonomous: true
requirements: [PLAT-04, PLAT-05]

must_haves:
  truths:
    - "install.sh installs bubblewrap and socat on Linux"
    - "install.sh supports apt, dnf, and pacman package managers"
    - "install.sh skips sandbox deps on macOS"
    - "install.sh no longer installs OpenShell"
  artifacts:
    - path: "install.sh"
      provides: "Platform-aware installer with Linux sandbox deps and no OpenShell"
      contains: "install_sandbox_deps"
  key_links:
    - from: "install.sh"
      to: "bubblewrap/socat packages"
      via: "apt-get/dnf/pacman install"
      pattern: "bubblewrap socat"
---

<objective>
Update install.sh to install bubblewrap and socat on Linux (apt/dnf/pacman) and remove all OpenShell installation code.

Purpose: New users on Linux get sandbox dependencies installed automatically. OpenShell is no longer part of the dependency chain.
Output: Updated install.sh with sandbox deps function and no OpenShell references.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-platform-compatibility/07-CONTEXT.md
@.planning/phases/07-platform-compatibility/07-RESEARCH.md
@install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace OpenShell install with sandbox deps in install.sh</name>
  <files>install.sh</files>
  <read_first>
    - install.sh
    - .planning/phases/07-platform-compatibility/07-CONTEXT.md
    - .planning/phases/07-platform-compatibility/07-RESEARCH.md
  </read_first>
  <action>
    Modify `install.sh`:

    1. Update the header comment (per D-18, D-19):
       - Change dependency list from "1. rightclaw, 2. process-compose, 3. OpenShell" to "1. rightclaw, 2. process-compose, 3. bubblewrap + socat (Linux only)"

    2. Delete `install_openshell()` function entirely (per D-18, D-19 / PLAT-05):
       - Remove the entire function block (lines ~162-180)

    3. Add `install_sandbox_deps()` function (per D-12, D-13, D-14, D-15, D-16 / PLAT-04):
       - Place it after `install_process_compose()` and before `run_init()`
       - On macOS (`$PLATFORM = "darwin"`): print message "macOS uses built-in Seatbelt sandbox (no additional deps needed)" via `ok` helper, return 0
       - On Linux: check if bwrap and socat are already installed via `command -v`
       - If both already present: print "bubblewrap and socat already installed", return 0
       - Build package list of only missing packages (only install what's needed)
       - Detect package manager via `command -v apt-get/dnf/pacman` (per D-15)
       - apt-get: `sudo apt-get install -y bubblewrap socat` (per D-14)
       - dnf: `sudo dnf install -y bubblewrap socat` (per D-14)
       - pacman: `sudo pacman -S --noconfirm bubblewrap socat` (per D-14)
       - No supported package manager: `die` with message listing manual install instructions
       - On success: print "sandbox dependencies installed" via `ok` helper

    4. Update `main()` function:
       - Replace `install_openshell` call with `install_sandbox_deps`
       - Keep `install_rightclaw`, `install_process_compose`, `check_bun`, `run_init`, `run_doctor` in same order

    5. Verify shell syntax is correct (per CLAUDE.md shell script rule).
  </action>
  <verify>
    <automated>cd /home/wb/dev/rightclaw && bash -n install.sh && echo "syntax ok" && ! rg "openshell|OpenShell" install.sh && echo "no openshell refs" && rg "install_sandbox_deps" install.sh >/dev/null && echo "sandbox_deps function exists" && rg "bubblewrap" install.sh >/dev/null && echo "bubblewrap present"</automated>
  </verify>
  <acceptance_criteria>
    - install.sh passes bash -n syntax check
    - install.sh contains `install_sandbox_deps()` function
    - install.sh contains no "openshell" or "OpenShell" references (case-insensitive rg)
    - install.sh contains "bubblewrap" and "socat" in install commands
    - install.sh contains "apt-get", "dnf", and "pacman" package manager branches
    - install.sh main() calls install_sandbox_deps (not install_openshell)
    - macOS branch returns early with Seatbelt message
  </acceptance_criteria>
  <done>
    - install.sh installs bubblewrap + socat on Linux via apt/dnf/pacman (PLAT-04)
    - install.sh skips sandbox deps on macOS with Seatbelt message (per D-16, D-17)
    - install.sh has zero OpenShell references (PLAT-05)
    - shell syntax is valid (bash -n passes)
  </done>
</task>

</tasks>

<verification>
- `bash -n install.sh` exits 0 (valid syntax)
- `rg -i "openshell" install.sh` returns no matches
- `rg "install_sandbox_deps" install.sh` returns matches
- `rg "bubblewrap" install.sh` returns matches
- `rg "apt-get\|dnf\|pacman" install.sh` returns matches for all three
</verification>

<success_criteria>
- install.sh installs bubblewrap and socat on Linux (apt/dnf/pacman)
- install.sh skips sandbox deps on macOS
- install.sh no longer references or installs OpenShell
- shell syntax is valid
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-compatibility/07-02-SUMMARY.md`
</output>
