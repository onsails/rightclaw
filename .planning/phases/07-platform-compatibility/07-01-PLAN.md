---
phase: 07-platform-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/rightclaw/src/doctor.rs
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03]

must_haves:
  truths:
    - "rightclaw doctor checks for bwrap and socat binaries on Linux"
    - "rightclaw doctor runs bwrap smoke test with --unshare-net on Linux when bwrap is found"
    - "rightclaw doctor skips bwrap/socat checks on macOS"
    - "rightclaw doctor does not check for openshell (already removed)"
  artifacts:
    - path: "crates/rightclaw/src/doctor.rs"
      provides: "Platform-conditional sandbox dependency checks + bwrap smoke test"
      contains: "check_bwrap_sandbox"
  key_links:
    - from: "crates/rightclaw/src/doctor.rs"
      to: "std::process::Command"
      via: "bwrap smoke test invocation"
      pattern: 'Command::new\("bwrap"\)'
    - from: "crates/rightclaw/src/doctor.rs"
      to: "std::env::consts::OS"
      via: "platform conditional"
      pattern: 'consts::OS == "linux"'
---

<objective>
Add Linux-specific sandbox dependency checks to `rightclaw doctor`: bwrap and socat binary detection, bwrap smoke test with AppArmor diagnostics.

Purpose: Users on Linux get clear diagnostics when sandbox dependencies are missing or AppArmor blocks bubblewrap user namespaces.
Output: Updated doctor.rs with platform-conditional checks and bwrap smoke test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-platform-compatibility/07-CONTEXT.md
@.planning/phases/07-platform-compatibility/07-RESEARCH.md
@crates/rightclaw/src/doctor.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From crates/rightclaw/src/doctor.rs:
```rust
pub enum CheckStatus { Pass, Fail, Warn }

pub struct DoctorCheck {
    pub name: String,
    pub status: CheckStatus,
    pub detail: String,
    pub fix: Option<String>,
}

pub fn run_doctor(home: &Path) -> Vec<DoctorCheck>;
fn check_binary(name: &str, fix_hint: Option<&str>) -> DoctorCheck;
fn check_agent_structure(home: &Path) -> Vec<DoctorCheck>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bwrap/socat binary checks and bwrap smoke test to doctor.rs</name>
  <files>crates/rightclaw/src/doctor.rs</files>
  <read_first>
    - crates/rightclaw/src/doctor.rs
    - .planning/phases/07-platform-compatibility/07-RESEARCH.md
  </read_first>
  <behavior>
    - Test: on Linux, run_doctor returns checks named "bwrap" and "socat" (verify via cfg(target_os = "linux") conditional test)
    - Test: on Linux, when bwrap binary is found, run_doctor returns a check named "bwrap-sandbox" (smoke test result)
    - Test: check_bwrap_sandbox() returns DoctorCheck with name "bwrap-sandbox"
    - Test: check_bwrap_sandbox() returns Fail status with fix guidance containing "apparmor" when bwrap smoke test fails
    - Test: check_bwrap_sandbox() fix guidance includes both AppArmor profile instructions and sysctl workaround
    - Test: existing test run_doctor_always_checks_all_three_binaries still passes (no openshell check)
    - Test: bwrap_fix_guidance() returns string containing "apparmor_parser", "sysctl", and Ubuntu docs link
  </behavior>
  <action>
    Modify `crates/rightclaw/src/doctor.rs`:

    1. Add `check_bwrap_sandbox()` function (per D-05, D-06, D-07, D-08, D-09):
       - Runs `bwrap --ro-bind / / --unshare-net --dev /dev true` via `std::process::Command` (research finding: must include `--unshare-net` to detect AppArmor network namespace restriction, per Pitfall 1 in RESEARCH.md)
       - Use `.stdout(Stdio::null()).stderr(Stdio::piped()).output()` to capture stderr without blocking
       - On success: return Pass with detail "bubblewrap sandbox functional"
       - On failure: parse stderr for "RTM_NEWADDR" / "Operation not permitted" (AppArmor) or "No permissions" (Debian userns)
       - Return Fail with fix guidance from `bwrap_fix_guidance()`

    2. Add `bwrap_fix_guidance()` function (per D-08):
       - Primary fix: AppArmor profile for bwrap at `/etc/apparmor.d/bwrap` with `userns,` permission and `abi <abi/4.0>` (per research)
       - Secondary fix: `sysctl -w kernel.apparmor_restrict_unprivileged_userns=0` (temporary)
       - Persistent sysctl fix via `/etc/sysctl.d/60-bwrap-userns.conf`
       - Include Ubuntu docs link: `https://ubuntu.com/blog/ubuntu-23-10-restricted-unprivileged-user-namespaces`

    3. Update `run_doctor()` to add platform-conditional checks (per D-01, D-02, D-03, D-04):
       - After the 3 existing binary checks (rightclaw, process-compose, claude), add:
       - `if std::env::consts::OS == "linux"` block
       - Inside: `check_binary("bwrap", Some("Install bubblewrap: sudo apt install bubblewrap (or dnf/pacman)"))`
       - Track whether bwrap was found (compare status == CheckStatus::Pass)
       - `check_binary("socat", Some("Install socat: sudo apt install socat (or dnf/pacman)"))`
       - If bwrap was found: `check_bwrap_sandbox()` (per D-09)
       - Keep existing `check_agent_structure(home)` call after all binary checks

    4. Add `PartialEq` derive to `CheckStatus` if not already present (needed for `bwrap_found` comparison) -- already present.

    5. Add tests:
       - `check_bwrap_sandbox_returns_doctor_check`: call function directly, verify it returns DoctorCheck with name "bwrap-sandbox" (will pass or fail depending on host, but must not panic)
       - `bwrap_fix_guidance_contains_apparmor_profile`: verify fix text contains "apparmor_parser", "/etc/apparmor.d/bwrap", "sysctl", and the Ubuntu docs URL
       - `#[cfg(target_os = "linux")] run_doctor_includes_bwrap_socat_on_linux`: verify run_doctor returns checks with names "bwrap" and "socat"
       - `#[cfg(not(target_os = "linux"))] run_doctor_skips_bwrap_socat_on_non_linux`: verify run_doctor does NOT return checks named "bwrap" or "socat"

    PLAT-03 is already satisfied (Phase 5 removed openshell from doctor.rs). Existing test `run_doctor_always_checks_all_three_binaries` already asserts `!binary_names.contains(&"openshell")`. No changes needed for PLAT-03.
  </action>
  <verify>
    <automated>cd /home/wb/dev/rightclaw && cargo test -p rightclaw --lib doctor -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - doctor.rs contains `fn check_bwrap_sandbox()` function
    - doctor.rs contains `fn bwrap_fix_guidance()` function
    - run_doctor() has `std::env::consts::OS == "linux"` conditional block
    - bwrap smoke test uses `--unshare-net --dev /dev` flags (not just `--ro-bind / /`)
    - fix guidance contains "apparmor_parser" and "sysctl" and "ubuntu.com"
    - all doctor tests pass including existing ones
  </acceptance_criteria>
  <done>
    - run_doctor() checks bwrap and socat on Linux, skips on macOS (per D-01 through D-04)
    - bwrap smoke test runs when bwrap found, reports AppArmor issues with fix guidance (per D-05 through D-09)
    - openshell remains absent from doctor checks (PLAT-03, verified by existing test)
    - all tests pass
  </done>
</task>

</tasks>

<verification>
- `cargo test -p rightclaw --lib doctor` passes all tests
- `cargo build --workspace` compiles cleanly
- `cargo clippy --workspace` has no new warnings
- `rg "openshell" crates/rightclaw/src/doctor.rs` returns no matches
- `rg "check_bwrap_sandbox" crates/rightclaw/src/doctor.rs` returns matches
- `rg 'consts::OS == "linux"' crates/rightclaw/src/doctor.rs` returns matches
</verification>

<success_criteria>
- rightclaw doctor checks bwrap + socat on Linux only
- bwrap smoke test includes --unshare-net for accurate AppArmor detection
- fix guidance provides both AppArmor profile and sysctl workaround
- no openshell references in doctor.rs
- all existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-compatibility/07-01-SUMMARY.md`
</output>
