---
status: complete
phase: 28.2-v3-0-uat-fix-teloxide-native-tls-and-doctor-async-runtime
source: [28.2-01-SUMMARY.md, 28.2-02-SUMMARY.md]
started: 2026-04-01T22:30:00Z
updated: 2026-04-01T22:30:00Z
---

## Current Test

number: 8
name: Cron Job Fires on Schedule
expected: |
  Create crons/hello.yaml with schedule "* * * * *" and a simple prompt. Wait ~1 min.
  A log appears in crons/logs/, and a cron_runs row is inserted with status success or failed.
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running rightclaw/process-compose instances. Clear leftover lock files or temp state. Run `rightclaw up` from scratch with a real bot token. The command should boot without errors: process-compose starts, bot process appears, and no panics or fatal errors are printed. The bot should stay running (not enter a restart loop).
result: pass
note: Fixed by two commits — resolve_token KEY=VALUE parsing + --telegram-allowed-chat-ids flag

### 2. Bot Stays Running — No TLS Restart Loop
expected: After `rightclaw up`, the bot process should connect to api.telegram.org successfully (TLS handshake with rustls). Bot should NOT enter a restart loop. Logs should NOT contain "error sending request" or "deleteWebhook failed". Bot stays up and responds to a test message.
result: pass

### 3. rightclaw doctor Webhook Check
expected: Run `rightclaw doctor`. The command should complete WITHOUT panicking ("Cannot start a runtime from within a runtime" was the old bug). It should print a webhook status check for agents with Telegram tokens — either a warning if a webhook is active, or a pass if no webhook is set.
result: pass
note: telegram-webhook/right ok — no active webhook. No panic.

### 4. Telegram Bot Receives Message and Replies
expected: Send a plain text message to the agent's Telegram bot. The bot should receive the message, dispatch it to Claude Code via `--agent <name>`, and send a reply back to Telegram within a reasonable time.
result: pass
note: Required fixing: --json-schema inline vs path, parse_reply_output structured_output field, enabledPlugins race (CC Telegram plugin competing with native bot for getUpdates). Root cause of inconsistency: settings.json had enabledPlugins causing every invoke_cc to start a competing Telegram poller.

### 5. Telegram Session Continuity
expected: Send two follow-up messages to the same bot in the same thread. The second message is answered in the context of the first — the CC session is reused, not restarted. The bot remembers context from the first message.
result: pass
note: Works after killing CC Telegram plugin processes and removing enabledPlugins from settings.json generation.

### 6. Telegram /reset Command
expected: Send `/reset` to the bot. Bot replies with a confirmation. Subsequent messages start a fresh CC session (no memory of previous conversation).
result: pass
note: /reset confirmed working. Menu now shows /start and /reset (CC Telegram plugin was overwriting commands — removed plugin + token from global CC config, added /start handler, dispatch.rs now uses delete_my_commands before set_my_commands).

### 7. Cron Runtime Starts With Bot
expected: Bot logs show the cron task started alongside the Telegram dispatcher on launch. Both "telegram" and "cron" references appear in startup logs.
result: pass
note: "cron task started agent=right" appears before "teloxide dispatcher starting".

### 8. Cron Job Fires on Schedule
expected: Create `crons/<job-name>.yaml` with `schedule: "* * * * *"` and a simple prompt. Wait ~1 min. A log appears in `crons/logs/`, and a `cron_runs` row is inserted with status success or failed.
result: pass
note: Job fired, completed (status=success), log written to crons/logs/, CC reply delivered to Telegram. Fixed: --dangerously-skip-permissions, USE_BUILTIN_RIPGREP=1, tokio::spawn in loop, --output-format json + --json-schema + parse structured_output + send to allowed_chat_ids.

### 9. Cron Lock Prevents Duplicate Runs
expected: If a cron job is still running when the next tick fires, a lock prevents a second subprocess from starting. Logs show a "lock is fresh, skipping" (or similar) message. No duplicate processes.
result: pass
note: "skipping — previous run still active (lock fresh)" at 13:13:00 and 13:15:00. lock-test ran 100s (sleep 90s), lock blocked two consecutive ticks. Fixed: run_job_loop was doing execute_job().await (sequential, ticks missed). Changed to tokio::spawn so loop continues ticking while job runs.

### 10. MCP cron_list_runs Tool
expected: Via rightclaw MCP server (from within an agent), call `cron_list_runs`. Returns a JSON list from the `cron_runs` table with fields: job_name, status, started_at, finished_at, log_path.
result: pass
note: Agent called cron_list_runs, returned 13 successful runs with correct fields. Fixed: parse_reply_output now handles plain-string CC result (CC ignores --json-schema after multi-turn MCP use).

### 11. MCP cron_show_run Tool
expected: Call `cron_show_run` with a valid run_id — returns the full row. Call with a non-existent ID — returns a "not found" message (not an MCP error/exception).
result: pass

### 12. cronsync Skill Creates Cron File
expected: Within a running agent, invoke the `/rightcron` skill with a job name, schedule, and prompt. Skill creates `crons/<job-name>.yaml` with the correct fields. Skill does NOT execute the job — file management only.
result: pass
note: daily-check.yaml created with schedule "0 9 * * *" and correct prompt. File-only, no immediate execution.

### 13. Cron Job Deletion and Schedule/Prompt Change
expected: |
  Delete a cron YAML → reconciler aborts the job loop on next 60s tick, no more fires.
  Change schedule or prompt in a YAML → reconciler aborts old loop, spawns new one with updated spec.
  Both tested via agent conversation (user asks bot to delete/modify, bot uses /rightcron skill).
result: pass

## Summary

total: 13
passed: 13
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "cron job CC output is parsed for tool calls and executed (e.g. reply sends to Telegram)"
  status: fixed
  fix: "execute_job now passes --output-format json + --json-schema (reply-schema.json), parses structured_output.content, sends to allowed_chat_ids if non-null. Confirmed working."
  test: 8

- truth: "CC sandbox enforced for cron and Telegram-invoked claude -p subprocesses"
  status: failed
  reason: "CC reports 'Sandbox disabled: dependencies missing — ripgrep not found at vendor path'. CC looks for its bundled rg inside nix store path. bubblewrap/socat may also be missing. sandbox.enabled=true in agent settings.json but CC silently disables it."
  severity: high
  test: 8
  root_cause: "CC sandbox requires its bundled ripgrep (from nix package), bubblewrap, and socat. rightclaw doctor checks bubblewrap+socat but NOT CC's bundled rg. Missing rg causes CC to disable sandbox entirely without error exit. Fix: add CC-bundled rg check to rightclaw doctor; or set sandbox.enabled=false when deps unavailable and warn loudly at rightclaw up time."
  artifacts:
    - path: "crates/bot/src/cron.rs"
      issue: "execute_job: claude -p invoked without --dangerously-skip-permissions (unlike worker.rs which was fixed)"
    - path: "crates/rightclaw/src/doctor.rs"
      issue: "doctor does not check CC bundled ripgrep — sandbox check incomplete"

- truth: "rightclaw up boots without restart loop — bot connects to api.telegram.org"
  status: failed
  reason: "User reported: restart loop — deleteWebhook failed: Telegram's error: Invalid bot token"
  severity: blocker
  test: 1
  root_cause: "resolve_token() in crates/bot/src/telegram/mod.rs reads .env file as raw string. init.rs writes TELEGRAM_BOT_TOKEN=<token>\\n (KEY=VALUE format). resolve_token returns full 'TELEGRAM_BOT_TOKEN=<token>' string to teloxide — Telegram rejects as invalid. Fix: parse KEY=VALUE in both file-reading paths of resolve_token."
  artifacts:
    - path: "crates/bot/src/telegram/mod.rs"
      issue: "resolve_token reads file with s.trim() — no KEY=VALUE parsing"
    - path: "crates/rightclaw/src/init.rs"
      issue: "init writes .env as TELEGRAM_BOT_TOKEN={token}\\n (correct for CC channel compat, but bot must parse it)"
