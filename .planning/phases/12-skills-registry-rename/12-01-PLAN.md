---
phase: 12-skills-registry-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/skills/SKILL.md
  - crates/rightclaw/src/codegen/skills.rs
  - crates/rightclaw/src/init.rs
  - crates/rightclaw-cli/src/main.rs
autonomous: true
requirements: [SKILLS-01, SKILLS-02, SKILLS-03, SKILLS-04, SKILLS-05]

must_haves:
  truths:
    - "No file, directory, constant, or string literal named 'clawhub' exists in the codebase"
    - "rightclaw up installs skills/SKILL.md into each agent's .claude/skills/skills/ directory"
    - "rightclaw up removes stale .claude/skills/clawhub/ from existing agent dirs on every run"
    - "All tests pass with the renamed paths"
  artifacts:
    - path: "skills/skills/SKILL.md"
      provides: "/skills built-in skill content (skills.sh manager)"
      min_lines: 50
    - path: "crates/rightclaw/src/codegen/skills.rs"
      provides: "install_builtin_skills() installing skills/SKILL.md"
      exports: ["install_builtin_skills"]
      contains: "SKILL_SKILLS"
    - path: "crates/rightclaw-cli/src/main.rs"
      provides: "cmd_up stale cleanup + updated test"
      contains: "skills/clawhub"
  key_links:
    - from: "crates/rightclaw/src/codegen/skills.rs"
      to: "skills/skills/SKILL.md"
      via: "include_str!()"
      pattern: "include_str!.*skills/skills/SKILL\\.md"
    - from: "crates/rightclaw-cli/src/main.rs"
      to: "agent.path.join(\".claude/skills/clawhub\")"
      via: "fs::remove_dir_all"
      pattern: "remove_dir_all.*clawhub"
---

<objective>
Rename all `clawhub` references to `skills` throughout the codebase: move `skills/clawhub/` to `skills/skills/`, rename the Rust constant `SKILL_CLAWHUB` to `SKILL_SKILLS`, update the `include_str!` path, update the `built_in_skills` slice entry from `"clawhub/SKILL.md"` to `"skills/SKILL.md"`, fix all test assertions, update the `println!` in `init.rs`, and add one-line stale dir cleanup in `cmd_up`.

Purpose: ClawHub is removed completely (not a fallback, not opt-in). Skills.sh is the sole registry. All agents get a `/skills` skill, not `/clawhub`.

Output: `skills/skills/SKILL.md` as the built-in skill source; zero remaining `clawhub` references in non-markdown source files; stale `.claude/skills/clawhub/` removed on every `rightclaw up`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-skills-registry-rename/12-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename clawhub to skills — source dir, constant, install path, all test assertions</name>
  <read_first>
    - skills/clawhub/SKILL.md (content to be preserved, just moved)
    - crates/rightclaw/src/codegen/skills.rs (SKILL_CLAWHUB constant, built_in_skills slice, all tests)
    - crates/rightclaw/src/init.rs (println! line 173, test assertion line 250)
    - crates/rightclaw-cli/src/main.rs (test lines 755-756)
  </read_first>
  <behavior>
    - installs_skills_skill test: dir.path().join(".claude/skills/skills/SKILL.md").exists() must be true
    - install_is_idempotent test: asserts skills/SKILL.md exists (not clawhub/SKILL.md)
    - skills_install_creates_builtin_skill_dirs in main.rs: asserts skills/SKILL.md exists
    - init_creates_default_agent_files: asserts .claude/skills/skills/SKILL.md exists
  </behavior>
  <files>
    skills/skills/SKILL.md
    crates/rightclaw/src/codegen/skills.rs
    crates/rightclaw/src/init.rs
    crates/rightclaw-cli/src/main.rs
  </files>
  <action>
    Step 1 — Move source skill directory (git mv preserves history):
      git mv skills/clawhub skills/skills

    Step 2 — Update crates/rightclaw/src/codegen/skills.rs:

      Line 3: Change
        const SKILL_CLAWHUB: &str = include_str!("../../../../skills/clawhub/SKILL.md");
      to:
        const SKILL_SKILLS: &str = include_str!("../../../../skills/skills/SKILL.md");

      Line 8 doc comment: Change
        /// Writes `clawhub/SKILL.md`, `rightcron/SKILL.md`, and `installed.json`.
      to:
        /// Writes `skills/SKILL.md`, `rightcron/SKILL.md`, and `installed.json`.

      Line 13 slice entry: Change
        ("clawhub/SKILL.md", SKILL_CLAWHUB),
      to:
        ("skills/SKILL.md", SKILL_SKILLS),

      Test fn installs_clawhub_skill (line 39): Rename to `installs_skills_skill`. Change assertion:
        dir.path().join(".claude/skills/clawhub/SKILL.md").exists(),
        "clawhub/SKILL.md should exist"
      to:
        dir.path().join(".claude/skills/skills/SKILL.md").exists(),
        "skills/SKILL.md should exist"

      Test fn install_is_idempotent (line 72): Change assertion:
        assert!(dir.path().join(".claude/skills/clawhub/SKILL.md").exists());
      to:
        assert!(dir.path().join(".claude/skills/skills/SKILL.md").exists());

    Step 3 — Update crates/rightclaw/src/init.rs:

      Line 173 println!: Change
        println!("  agents/right/.claude/skills/clawhub/SKILL.md  (skills.sh manager)");
      to:
        println!("  agents/right/.claude/skills/skills/SKILL.md  (skills.sh manager)");

      Line 250 test assertion: Change
        agents_dir.join(".claude/skills/clawhub/SKILL.md").exists(),
        "clawhub skill should be installed"
      to:
        agents_dir.join(".claude/skills/skills/SKILL.md").exists(),
        "skills skill should be installed"

    Step 4 — Update crates/rightclaw-cli/src/main.rs lines 755-756:

      Change
        let clawhub_skill = skills_dir.join("clawhub").join("SKILL.md");
        assert!(clawhub_skill.exists(), "clawhub/SKILL.md should be installed");
      to:
        let skills_skill = skills_dir.join("skills").join("SKILL.md");
        assert!(skills_skill.exists(), "skills/SKILL.md should be installed");
  </action>
  <verify>
    <automated>cd /home/wb/dev/rightclaw && cargo test -p rightclaw --lib codegen::skills::tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - skills/skills/SKILL.md exists: `ls /home/wb/dev/rightclaw/skills/skills/SKILL.md` returns file
    - skills/clawhub/ directory does NOT exist: `ls /home/wb/dev/rightclaw/skills/clawhub/` returns error
    - skills.rs contains `SKILL_SKILLS`: `rg "SKILL_SKILLS" crates/rightclaw/src/codegen/skills.rs` returns match
    - skills.rs contains NO `SKILL_CLAWHUB`: `rg "SKILL_CLAWHUB" crates/rightclaw/src/codegen/skills.rs` returns empty
    - skills.rs contains `skills/skills/SKILL.md` in include_str!: `rg 'include_str!.*skills/skills/SKILL' crates/rightclaw/src/codegen/skills.rs` returns match
    - skills.rs contains `"skills/SKILL.md"` in built_in_skills: `rg '"skills/SKILL\.md"' crates/rightclaw/src/codegen/skills.rs` returns match
    - init.rs contains NO `clawhub`: `rg "clawhub" crates/rightclaw/src/init.rs` returns empty
    - main.rs test contains `skills/SKILL.md should be installed`: `rg 'skills/SKILL.md should be installed' crates/rightclaw-cli/src/main.rs` returns match
    - cargo test for skills.rs unit tests passes: all tests in `codegen::skills::tests` pass
  </acceptance_criteria>
  <done>All clawhub string literals removed from skills.rs, init.rs, main.rs. skills/skills/SKILL.md exists. All codegen::skills tests pass with updated path assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add stale .claude/skills/clawhub/ cleanup in cmd_up (SKILLS-05)</name>
  <read_first>
    - crates/rightclaw-cli/src/main.rs (lines 385-401: the agent loop step 8, install_builtin_skills call at line 392)
  </read_first>
  <behavior>
    - When .claude/skills/clawhub/ exists in an agent dir before cmd_up runs, it is gone afterward
    - When .claude/skills/clawhub/ does NOT exist, cmd_up runs without error (idempotent)
    - The cleanup does not propagate errors — fs::remove_dir_all().ok() pattern
  </behavior>
  <files>
    crates/rightclaw-cli/src/main.rs
  </files>
  <action>
    In crates/rightclaw-cli/src/main.rs, locate the agent preparation loop step 8 (around line 390-392):

      // 8. Reinstall built-in skills (Phase 9, AENV-03).
      // Always overwrites built-in skill dirs; user skill dirs untouched (D-10).
      rightclaw::codegen::install_builtin_skills(&agent.path)?;

    Insert a stale cleanup line BEFORE the install_builtin_skills call:

      // 8. Reinstall built-in skills (Phase 9, AENV-03).
      // Always overwrites built-in skill dirs; user skill dirs untouched (D-10).
      // Remove stale clawhub dir from agents upgraded from pre-v2.2 (SKILLS-05, D-01, D-02).
      let _ = std::fs::remove_dir_all(agent.path.join(".claude/skills/clawhub"));
      rightclaw::codegen::install_builtin_skills(&agent.path)?;

    No other changes. The `let _ =` pattern intentionally ignores the error — this is a best-effort
    cleanup, not a data operation (per D-01 in CONTEXT.md). This is the ONLY place in the codebase
    where error ignoring is acceptable.

    Also add a test to the existing skills tests block in main.rs (around line 744-757) to verify the cleanup:

      #[test]
      fn cmd_up_removes_stale_clawhub_skill_dir() {
          let tmp = TempDir::new().unwrap();
          let agent_dir = make_agent_dir(&tmp, "agent-stale");

          // Simulate pre-v2.2 state: clawhub dir exists
          let stale = agent_dir.join(".claude").join("skills").join("clawhub");
          std::fs::create_dir_all(&stale).unwrap();
          std::fs::write(stale.join("SKILL.md"), "old content").unwrap();
          assert!(stale.exists(), "stale dir should exist before cleanup");

          // Run cleanup (same logic as cmd_up inserts)
          let _ = std::fs::remove_dir_all(agent_dir.join(".claude/skills/clawhub"));

          assert!(!stale.exists(), "stale clawhub dir should be removed after cleanup");
      }

      #[test]
      fn stale_cleanup_is_idempotent_when_dir_absent() {
          let tmp = TempDir::new().unwrap();
          let agent_dir = make_agent_dir(&tmp, "agent-no-stale");
          // No clawhub dir — cleanup should not error
          let result = std::fs::remove_dir_all(agent_dir.join(".claude/skills/clawhub"));
          // Either Ok or NotFound error — never panics
          assert!(result.is_ok() || result.unwrap_err().kind() == std::io::ErrorKind::NotFound);
      }
  </action>
  <verify>
    <automated>cd /home/wb/dev/rightclaw && cargo test -p rightclaw-cli 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - main.rs contains the stale cleanup line: `rg 'remove_dir_all.*\.claude/skills/clawhub' crates/rightclaw-cli/src/main.rs` returns match
    - cleanup line uses `let _ =` (error ignored): `rg 'let _ = std::fs::remove_dir_all' crates/rightclaw-cli/src/main.rs` returns match
    - cleanup line appears before `install_builtin_skills` call in the agent loop: manual read confirms ordering
    - cargo test -p rightclaw-cli passes with both new tests present
    - rg "clawhub" over all non-markdown source files returns zero matches outside the remove_dir_all cleanup line: `rg "clawhub" crates/ --type rust | rg -v "remove_dir_all"` returns empty
  </acceptance_criteria>
  <done>Stale cleanup line present in cmd_up agent loop before install_builtin_skills. Both idempotency tests pass. Zero clawhub references remain in Rust source except the remove_dir_all cleanup.</done>
</task>

</tasks>

<verification>
After both tasks complete, run full workspace build and tests:

```bash
cd /home/wb/dev/rightclaw && cargo build --workspace 2>&1 | tail -20
cd /home/wb/dev/rightclaw && cargo test --workspace 2>&1 | grep -E "FAILED|passed|error"
rg "clawhub" /home/wb/dev/rightclaw/crates/ --type rust | rg -v "remove_dir_all"
ls /home/wb/dev/rightclaw/skills/
```

Expected:
- `cargo build --workspace` exits 0
- `cargo test --workspace` shows no FAILED tests
- `rg "clawhub" crates/ --type rust | rg -v "remove_dir_all"` returns empty (zero stray references)
- `ls skills/` shows `cronsync/` and `skills/` (no `clawhub/`)
</verification>

<success_criteria>
1. `skills/clawhub/` directory does not exist; `skills/skills/SKILL.md` exists with identical content
2. `SKILL_CLAWHUB` constant is gone; `SKILL_SKILLS` constant exists in skills.rs
3. `install_builtin_skills()` installs `skills/SKILL.md` (not `clawhub/SKILL.md`) into each agent's `.claude/skills/skills/`
4. `cmd_up` silently removes `.claude/skills/clawhub/` from each agent dir before reinstalling skills
5. All tests pass (`cargo test --workspace` exits 0)
6. Zero `clawhub` string literals remain in Rust source files except the one `remove_dir_all` cleanup call
</success_criteria>

<output>
After completion, create `.planning/phases/12-skills-registry-rename/12-01-SUMMARY.md`
</output>
