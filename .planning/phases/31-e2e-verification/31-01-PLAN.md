# Plan 31-01: E2E Sandbox Verification Script

**Phase:** 31 — e2e-verification
**Goal:** Deliver `tests/e2e/verify-sandbox.sh` — a repeatable bash script that proves sandbox is engaged for a live agent via a three-stage pipeline: doctor pre-flight, dependency PATH check, CC smoke test with structured JSON output.
**Requirements:** VER-01, VER-02, VER-03

---

## Tasks

### Task 1: Create `tests/e2e/` directory and write `verify-sandbox.sh`

**Files:**
- `tests/e2e/verify-sandbox.sh` (create)

**Action:**

Create `tests/e2e/` directory and write the following script verbatim. This is the complete implementation — no pseudocode, no placeholders.

```bash
#!/usr/bin/env bash
# verify-sandbox.sh — Verify rightclaw CC sandbox engagement for a live agent.
#
# Usage:
#   tests/e2e/verify-sandbox.sh <agent-name>
#
# Requires:
#   - rightclaw up has been run for <agent-name>
#   - rightclaw, claude, rg, socat, bwrap in PATH
#
# Exit codes:
#   0 = all stages passed (sandbox confirmed engaged)
#   1 = one or more stages failed

set -euo pipefail

# ---------------------------------------------------------------------------
# Argument parsing
# ---------------------------------------------------------------------------
AGENT_NAME="${1:?Usage: verify-sandbox.sh <agent-name>}"
RIGHTCLAW_HOME="${RIGHTCLAW_HOME:-$HOME/.rightclaw}"
AGENT_DIR="$RIGHTCLAW_HOME/agents/$AGENT_NAME"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LOG_FILE="$SCRIPT_DIR/last-run.log"

# ---------------------------------------------------------------------------
# Color output (disabled when stdout is not a tty)
# ---------------------------------------------------------------------------
if [ -t 1 ]; then
  GREEN='\033[0;32m'
  RED='\033[0;31m'
  YELLOW='\033[1;33m'
  NC='\033[0m'
else
  GREEN=''
  RED=''
  YELLOW=''
  NC=''
fi

PASS_COUNT=0
FAIL_COUNT=0

pass() {
  echo -e "${GREEN}[PASS]${NC} $*"
  PASS_COUNT=$((PASS_COUNT + 1))
}

fail() {
  echo -e "${RED}[FAIL]${NC} $*"
  FAIL_COUNT=$((FAIL_COUNT + 1))
}

warn() {
  echo -e "${YELLOW}[WARN]${NC} $*"
}

# ---------------------------------------------------------------------------
# Stage 1: rightclaw doctor pre-flight
# ---------------------------------------------------------------------------
echo ""
echo "=== Stage 1: Doctor pre-flight ==="

DOCTOR_OUTPUT=""
DOCTOR_EXIT=0
DOCTOR_OUTPUT=$(rightclaw doctor 2>&1) || DOCTOR_EXIT=$?

echo "$DOCTOR_OUTPUT"

if [ $DOCTOR_EXIT -ne 0 ]; then
  fail "rightclaw doctor exited non-zero ($DOCTOR_EXIT) — aborting before CC smoke test"
  echo ""
  echo "=== Summary ==="
  echo -e "${RED}FAILED${NC}: doctor pre-flight failed. Fix issues above, then re-run."
  exit 1
fi

if echo "$DOCTOR_OUTPUT" | grep -q ' FAIL '; then
  fail "rightclaw doctor output contains FAIL checks — aborting before CC smoke test"
  echo ""
  echo "=== Summary ==="
  echo -e "${RED}FAILED${NC}: doctor reported failures. Fix issues above, then re-run."
  exit 1
fi

pass "rightclaw doctor: all checks passed (no FAIL)"

# ---------------------------------------------------------------------------
# Stage 2: Dependency availability in PATH
# ---------------------------------------------------------------------------
echo ""
echo "=== Stage 2: Sandbox dependency PATH check ==="

DEPS_OK=true

for dep in rg socat bwrap; do
  if command -v "$dep" > /dev/null 2>&1; then
    pass "$dep: found at $(command -v "$dep")"
  else
    fail "$dep: not found in PATH"
    DEPS_OK=false
  fi
done

if [ "$DEPS_OK" = false ]; then
  echo ""
  echo "=== Summary ==="
  echo -e "${RED}FAILED${NC}: one or more sandbox dependencies missing from PATH. Install missing tools and re-run."
  exit 1
fi

# ---------------------------------------------------------------------------
# Stage 3: settings.json pre-flight
# ---------------------------------------------------------------------------
echo ""
echo "=== Stage 3: Agent settings pre-flight ==="

SETTINGS_FILE="$AGENT_DIR/.claude/settings.json"
REPLY_SCHEMA_FILE="$AGENT_DIR/.claude/reply-schema.json"

if [ ! -f "$SETTINGS_FILE" ]; then
  fail "settings.json not found at $SETTINGS_FILE"
  echo "  run 'rightclaw up' first to generate agent settings"
  echo ""
  echo "=== Summary ==="
  echo -e "${RED}FAILED${NC}: agent not initialized. Run 'rightclaw up' then re-run this script."
  exit 1
fi
pass "settings.json exists"

if [ ! -f "$REPLY_SCHEMA_FILE" ]; then
  fail "reply-schema.json not found at $REPLY_SCHEMA_FILE"
  echo "  run 'rightclaw up' first to generate agent files"
  echo ""
  echo "=== Summary ==="
  echo -e "${RED}FAILED${NC}: agent not initialized. Run 'rightclaw up' then re-run this script."
  exit 1
fi
pass "reply-schema.json exists"

# ---------------------------------------------------------------------------
# Stage 4: CC smoke test — sandbox engagement
# ---------------------------------------------------------------------------
echo ""
echo "=== Stage 4: CC smoke test (sandbox engagement) ==="
echo "  agent:   $AGENT_NAME"
echo "  dir:     $AGENT_DIR"
echo "  model:   haiku"
echo "  log:     $LOG_FILE"
echo ""

REPLY_SCHEMA="$(cat "$REPLY_SCHEMA_FILE")"
PROMPT="Reply with a single word: ok"

# Use explicit exit code capture — set -e must not kill the script on CC non-zero exit.
CC_EXIT=0
CC_OUTPUT=""
CC_OUTPUT=$(
  HOME="$AGENT_DIR" \
  USE_BUILTIN_RIPGREP=0 \
  claude -p \
    --dangerously-skip-permissions \
    --output-format json \
    --agent "$AGENT_NAME" \
    --model haiku \
    --json-schema "$REPLY_SCHEMA" \
    -- "$PROMPT" \
  2>"$LOG_FILE"
) || CC_EXIT=$?

if [ $CC_EXIT -ne 0 ]; then
  fail "claude exited with code $CC_EXIT — sandbox or CC failure"
  echo ""
  echo "  CC stderr output (from $LOG_FILE):"
  echo "  ---"
  sed 's/^/  /' "$LOG_FILE"
  echo "  ---"
  echo ""
  echo "  Possible causes:"
  echo "  - Sandbox failed to start (check bwrap AppArmor restrictions on Ubuntu 24.04+)"
  echo "  - failIfUnavailable:true in settings.json caused fatal sandbox error"
  echo "  - CC binary not found or license not accepted"
  echo "  - settings.json misconfigured (run 'rightclaw up' to regenerate)"
  echo ""
  echo "=== Summary ==="
  echo -e "${RED}FAILED${NC}: CC smoke test failed (exit $CC_EXIT). See log at $LOG_FILE"
  exit 1
fi

# Validate JSON output
if echo "$CC_OUTPUT" | python3 -c "import json,sys; json.load(sys.stdin)" > /dev/null 2>&1; then
  pass "CC smoke test: exit 0 + valid JSON output — sandbox confirmed engaged"
  echo "  output: $CC_OUTPUT"
  # VER-01/VER-02 proof: settings.json has failIfUnavailable:true, so CC exit 0 means
  # sandbox did NOT fail. If bwrap/Seatbelt failed to engage, CC would exit non-zero.
  # We explicitly do NOT grep stderr for sandbox warning strings (brittle across CC versions,
  # see 31-DISCUSSION-LOG.md). Belt-and-suspenders: scan stderr for "sandbox" keyword as
  # informational only — does not affect exit code.
  if [ -s "$LOG_FILE" ] && grep -qi "sandbox" "$LOG_FILE"; then
    warn "CC stderr mentions 'sandbox' — review $LOG_FILE to confirm no degradation"
  fi
else
  fail "CC exited 0 but output is not valid JSON"
  echo "  raw output: $CC_OUTPUT"
  echo ""
  echo "=== Summary ==="
  echo -e "${RED}FAILED${NC}: CC output parse error. Check $LOG_FILE for stderr."
  exit 1
fi

# ---------------------------------------------------------------------------
# Summary
# ---------------------------------------------------------------------------
echo ""
echo "=== Summary ==="
TOTAL=$((PASS_COUNT + FAIL_COUNT))
echo "  $PASS_COUNT/$TOTAL checks passed"
echo ""

if [ $FAIL_COUNT -eq 0 ]; then
  echo -e "${GREEN}ALL CHECKS PASSED${NC}"
  echo "  VER-01: bot subprocess sandbox confirmed — exit 0 under failIfUnavailable:true"
  echo "          (same claude binary + settings.json as teloxide worker)"
  echo "  VER-02: cron subprocess sandbox confirmed — exit 0 under failIfUnavailable:true"
  echo "          (same claude binary + settings.json as cron runner)"
  echo "  VER-03: rg + socat + bwrap confirmed in PATH"
  echo ""
  echo "  CC stderr log saved to: $LOG_FILE"
  exit 0
else
  echo -e "${RED}$FAIL_COUNT CHECK(S) FAILED${NC}"
  exit 1
fi
```

After writing the file, run `chmod +x tests/e2e/verify-sandbox.sh`.

Also create `tests/e2e/.gitignore` with content:
```
last-run.log
```
This prevents `last-run.log` (written on every run per D-09) from being accidentally committed.

**Notes on implementation choices:**

- `set -euo pipefail` is at the top, but Stage 4 uses `CC_EXIT=0 / ... || CC_EXIT=$?` pattern to capture the CC exit code without triggering `set -e`. This is the correct pattern for non-zero exit inspection under strict mode.
- Doctor output parsing: exit code check is the primary signal (doctor exits non-zero on any Fail per `main.rs:265-269`). The secondary `grep -q ' FAIL '` is belt-and-suspenders documentation — technically redundant since doctor exit already captures all Fail cases, but kept to make the check intent self-evident.
- Doctor `grep ' FAIL '` matches the literal format from `doctor.rs:30`: `write!(f, "  {:<20} {:<6} {}", ...)` where status `"FAIL"` padded to 6 chars renders as `FAIL  ` — the sequence ` FAIL ` (space-FAIL-space) is present in all Fail lines.
- `command -v` is POSIX-portable and works under `set -u` unlike `which`.
- CC invocation mirrors `worker.rs:371-408` exactly: `HOME=<agent_dir>`, `USE_BUILTIN_RIPGREP=0`, `--dangerously-skip-permissions`, `--output-format json`, `--agent`, `--model haiku`, `--json-schema` with inlined schema content (not a file path — CC expects inline JSON string per worker.rs:394), cwd implicit via `HOME`.
- `cwd` for the CC invocation: worker.rs sets `current_dir(&ctx.agent_dir)`. This script achieves the same by `cd`-ing implicitly through `HOME` override. For strict equivalence, wrap in a subshell: `( cd "$AGENT_DIR" && HOME=... claude ... )`. Adjust if needed — the key env vars and flags are what drive sandbox behavior.
- `reply-schema.json` is read and inlined per the worker.rs pattern (line 392-394). The schema is the standard reply schema deployed by `rightclaw up`.
- `last-run.log` is always written (on both success and failure) for audit trail, per D-09.

**Verify:**
```bash
bash -n tests/e2e/verify-sandbox.sh
# exits 0 = syntax valid

tests/e2e/verify-sandbox.sh
# exits 1 with: "Usage: verify-sandbox.sh <agent-name>" (no args = usage error via :?)
```

**Done:** `tests/e2e/verify-sandbox.sh` exists, is executable, passes `bash -n` syntax check, prints usage error when called with no arguments.

---

### Task 2: Wire subshell cwd for exact CC invocation match

**Files:**
- `tests/e2e/verify-sandbox.sh` (edit Stage 4 CC invocation)

**Action:**

The CC invocation in Task 1 sets `HOME` but not `cwd`. Worker.rs sets both (`cmd.env("HOME", ...)` and `cmd.current_dir(...)`). Update Stage 4 to run CC from `$AGENT_DIR` using a subshell so both HOME and cwd match the production invocation:

Replace the Stage 4 CC invocation block with:

```bash
CC_OUTPUT=$(
  cd "$AGENT_DIR" && \
  HOME="$AGENT_DIR" \
  USE_BUILTIN_RIPGREP=0 \
  claude -p \
    --dangerously-skip-permissions \
    --output-format json \
    --agent "$AGENT_NAME" \
    --model haiku \
    --json-schema "$REPLY_SCHEMA" \
    -- "$PROMPT" \
  2>"$LOG_FILE"
) || CC_EXIT=$?
```

The subshell `( cd ... && ... )` isolates the `cd` from the outer script — `set -e` applies inside the subshell but the outer `|| CC_EXIT=$?` captures failure at subshell boundary.

**Note:** This task may be collapsed into Task 1 during execution if the executor writes the full script at once with the subshell already included. The separation here is for clarity of intent.

**Verify:**
```bash
bash -n tests/e2e/verify-sandbox.sh
```

**Done:** Script syntax valid, Stage 4 CC invocation uses subshell with `cd "$AGENT_DIR"` so cwd matches production worker.rs behavior.

---

## Verification

After execution, manually verify:

1. **Syntax check passes:**
   ```bash
   bash -n /home/wb/dev/rightclaw/tests/e2e/verify-sandbox.sh
   ```
   Should exit 0 silently.

2. **Usage error fires:**
   ```bash
   tests/e2e/verify-sandbox.sh
   # Expected: "verify-sandbox.sh: 1: Usage: verify-sandbox.sh <agent-name>"
   # or zsh equivalent — exits 1
   ```

3. **Doctor abort on missing agent:**
   ```bash
   RIGHTCLAW_HOME=/tmp/nonexistent tests/e2e/verify-sandbox.sh fake-agent
   # Expected: doctor runs (may succeed), settings.json check fires, prints
   # "run 'rightclaw up' first", exits 1
   ```

4. **Full run (requires live agent):**
   ```bash
   # Precondition: rightclaw up has been run with a real agent
   tests/e2e/verify-sandbox.sh <your-agent-name>
   # Expected: all PASS, "ALL CHECKS PASSED" at end, exit 0
   # CC stderr saved to tests/e2e/last-run.log
   ```

---

## Canonical References

- `crates/bot/src/telegram/worker.rs:371-408` — CC invocation pattern (HOME, USE_BUILTIN_RIPGREP, flags)
- `crates/rightclaw/src/doctor.rs:23-30` — Doctor output format (`{:<20} {:<6}` with status literals ok/FAIL/warn)
- `crates/rightclaw-cli/src/main.rs:247-271` — Doctor exit code behavior (non-zero on any Fail check)
- `.planning/phases/31-e2e-verification/31-CONTEXT.md` — Phase decisions D-01 through D-09
