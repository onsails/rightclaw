---
phase: 13-policy-gate-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/skills/SKILL.md
autonomous: true
requirements:
  - GATE-01
  - GATE-02

must_haves:
  truths:
    - "skills/skills/SKILL.md contains no references to policy.yaml, metadata.openclaw, or OpenShell"
    - "Step 3 in the install command instructs agent to read compatibility prose and classify bins/env/network/filesystem requirements"
    - "Step 3 BLOCKs on missing network domains and missing allowWrite paths; WARNs only on bins and env vars"
    - "Step 2 checks skills.sh and registry.npmjs.org in allowedDomains before running npx skills add, with the exact warn message from CONTEXT.md"
    - "The compatibility frontmatter field reads: Requires Node.js (npx), internet access to skills.sh and npmjs.org"
    - "A skill-doctor command section exists under ## Commands with a bash-block-driven audit loop and a table output format"
    - "skill-doctor scans both installed.json and .claude/skills/*/SKILL.md on disk (labels untracked as manual)"
  artifacts:
    - path: "skills/skills/SKILL.md"
      provides: "Rewritten policy gate (Step 3), pre-install domain check (Step 2), skill-doctor command, updated compatibility field"
      contains: "compatibility: Requires Node.js (npx), internet access to skills.sh and npmjs.org"
  key_links:
    - from: "Step 3 gate logic"
      to: ".claude/settings.json sandbox.network.allowedDomains"
      via: "cat .claude/settings.json"
      pattern: "allowedDomains"
    - from: "Step 3 gate logic"
      to: ".claude/settings.json sandbox.filesystem.allowWrite"
      via: "cat .claude/settings.json"
      pattern: "allowWrite"
    - from: "skill-doctor"
      to: ".claude/skills/installed.json"
      via: "cat .claude/skills/installed.json"
      pattern: "installed.json"
---

<objective>
Rewrite `skills/skills/SKILL.md` so the policy gate reflects CC-native sandbox reality. Drops all OpenShell/policy.yaml references. Adds pre-install domain check to Step 2. Replaces Step 3 entirely with compatibility-prose-based gate logic. Adds new skill-doctor command. Updates frontmatter compatibility field.

Purpose: GATE-01 and GATE-02 — the old Step 3 references non-standard `metadata.openclaw.requires.*` fields that no longer exist. The new gate reads the standard `compatibility` prose field and checks it against the CC-native sandbox's `settings.json`.
Output: `skills/skills/SKILL.md` with rewritten Step 2 (pre-install check) + Step 3 (policy gate) + new skill-doctor command + updated frontmatter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-policy-gate-rework/13-CONTEXT.md
@.planning/phases/13-policy-gate-rework/13-RESEARCH.md

<interfaces>
<!-- From skills/skills/SKILL.md — key structural facts for executor -->

Frontmatter (lines 1-9): name, description, version fields. `compatibility` field is ABSENT — must be added per D-07.

File structure (what changes and what stays):
- frontmatter: ADD `compatibility: Requires Node.js (npx), internet access to skills.sh and npmjs.org`
- ## When to Activate: UNCHANGED
- ## Configuration: UNCHANGED
- ## Commands > ### search: UNCHANGED
- ## Commands > ### install <slug>:
  - Step 1: UNCHANGED
  - Step 2: ADD pre-install domain check (D-06) BEFORE the `npx skills add` command
  - Step 3 (lines 95-122): FULL REPLACEMENT (drop metadata.openclaw entirely)
  - Step 4: UNCHANGED
  - Step 5: UNCHANGED
- ## Commands > ### remove: UNCHANGED
- ## Commands > ### list: UNCHANGED
- ## Commands > ### update: UNCHANGED
- ## Commands > ### skill-doctor: NEW SECTION (D-08 through D-10)
- ## Error Handling: UNCHANGED
- ## Important Rules: Rule 2 language update (GATE-01 compliance)

settings.json field paths confirmed from settings.rs:
```json
{
  "sandbox": {
    "filesystem": { "allowWrite": ["<path>"] },
    "network": { "allowedDomains": ["api.anthropic.com", "github.com", "npmjs.org", ...] }
  }
}
```

DEFAULT_ALLOWED_DOMAINS includes: api.anthropic.com, github.com, npmjs.org, crates.io, agentskills.io, api.telegram.org
skills.sh is NOT in DEFAULT_ALLOWED_DOMAINS — D-06 warning is warranted.
npmjs.org IS in DEFAULT_ALLOWED_DOMAINS but registry.npmjs.org (subdomain) may not be covered — warn for both.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite frontmatter, Step 2 domain pre-check, and Step 3 policy gate</name>
  <read_first>
    - skills/skills/SKILL.md — read in full before any edit; understand current Step 2 and Step 3 structure (lines 76-122)
    - .planning/phases/13-policy-gate-rework/13-CONTEXT.md — locked decisions D-01 through D-07, exact warn message wording for D-06, BLOCK/WARN tier rules
    - .planning/phases/13-policy-gate-rework/13-RESEARCH.md — architecture diagram, pitfall list, open questions with recommendations
  </read_first>
  <files>skills/skills/SKILL.md</files>
  <action>
Make three targeted edits to skills/skills/SKILL.md:

**Edit 1 — Add compatibility to frontmatter (D-07)**

After the `version: 0.3.0` line, add:
```
compatibility: Requires Node.js (npx), internet access to skills.sh and npmjs.org
```

**Edit 2 — Pre-install domain check in Step 2 (D-06)**

In the `### install <slug>` section, after the Step 2 heading and `npx skills add` command block but BEFORE "**Step 3:**", insert this new block:

```
**Pre-install domain check**

Before running `npx skills add`, verify that the install command itself can reach its required domains.

Read `.claude/settings.json` and check `sandbox.network.allowedDomains`:

```bash
cat .claude/settings.json
```

- If `skills.sh` is absent from `allowedDomains`: warn and stop.
  > Warning: `skills.sh` is not in your sandbox `allowedDomains`. Add it to your `agent.yaml` sandbox overrides: `allowed_domains: [skills.sh, registry.npmjs.org]`
  > After updating `agent.yaml`, run `rightclaw up` to regenerate `settings.json`, then retry.
- If `registry.npmjs.org` (or `npmjs.org`) is absent: add the same warning.
- If both are present: proceed with `npx skills add`.
```

**Edit 3 — Replace Step 3 in full (GATE-01 + GATE-02)**

Replace everything from the `**Step 3: Policy gate audit**` heading through `**If all checks pass** (or the skill has no special requirements): proceed to Step 4.` with the following:

```
**Step 3: Policy gate audit**

After the skill files are downloaded to `.claude/skills/<name>/`, read the skill's SKILL.md and check its `compatibility` field:

```bash
head -20 .claude/skills/<name>/SKILL.md
```

**If the skill has no `compatibility` field:** treat as no requirements — proceed directly to Step 4.

**If a `compatibility` field is present:** interpret it as natural language to identify requirement categories:

- **Bins:** binary names (e.g., `git`, `docker`, `python3`, `node`, `jq`) — any named executable tool
- **Env vars:** ALL_CAPS_WITH_UNDERSCORES tokens that are not domain names (e.g., `OPENAI_API_KEY`, `GITHUB_TOKEN`)
- **Network:** domain-like tokens containing dots (e.g., `api.openai.com`, `registry.npmjs.org`)
- **Filesystem write:** any phrase like "read-write access", "writes to", "modifies files outside", "filesystem write"

Run the following checks:

```bash
# Read the agent's sandbox configuration
cat .claude/settings.json

# Check each required binary
which <bin>

# Check each required env var
printenv <VAR>
```

Apply the two-tier gate:

**BLOCK (sandbox-enforced — do not proceed):**
- A required network domain is NOT present in `sandbox.network.allowedDomains` in `.claude/settings.json`
- A required filesystem write path is NOT covered by `sandbox.filesystem.allowWrite` in `.claude/settings.json`

**WARN only (advisory — proceed after informing the user):**
- A required binary is not found in PATH (`which` returns nothing)
- A required env var is not set (`printenv` returns nothing)

Display the full audit result as a table before deciding to proceed or block:

| Permission | Required | Status |
|------------|----------|--------|
| Binary: git | Yes | OK |
| Env: OPENAI_API_KEY | Yes | WARN — not set |
| Network: api.openai.com | Yes | BLOCK — not in allowedDomains |
| Filesystem: read-write | Yes | OK |

**If any BLOCK condition is present:**
> Installation blocked. The skill requires network or filesystem access that your agent's sandbox does not allow. Add the missing domains or write paths to your `agent.yaml` sandbox section, then run `rightclaw up` to regenerate `settings.json`, and retry the installation.

Do NOT auto-expand the agent's sandbox config. The user must explicitly update `agent.yaml`.

**If no BLOCK conditions (WARNs only or all pass):** inform the user of any warnings, then proceed to Step 4.
```

**Edit 4 — Update Important Rules rule 2**

Change rule 2 from:
> Never install a skill without running the policy gate audit first. No exceptions.

To:
> Never install a skill without running the pre-install domain check (Step 2) and the policy gate audit (Step 3) first. No exceptions.
  </action>
  <verify>
    <automated>
      rg "compatibility:" /home/wb/dev/rightclaw/skills/skills/SKILL.md | head -5
      rg "metadata.openclaw" /home/wb/dev/rightclaw/skills/skills/SKILL.md | wc -l
      rg "policy.yaml" /home/wb/dev/rightclaw/skills/skills/SKILL.md | wc -l
      rg "allowedDomains" /home/wb/dev/rightclaw/skills/skills/SKILL.md | head -5
      rg "allowWrite" /home/wb/dev/rightclaw/skills/skills/SKILL.md | head -5
      rg "skills.sh.*not in your sandbox" /home/wb/dev/rightclaw/skills/skills/SKILL.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg "compatibility:" skills/skills/SKILL.md` returns a line containing `Requires Node.js (npx), internet access to skills.sh and npmjs.org`
    - `rg "metadata.openclaw" skills/skills/SKILL.md | wc -l` returns `0`
    - `rg "policy.yaml" skills/skills/SKILL.md | wc -l` returns `0`
    - `rg "OpenShell" skills/skills/SKILL.md | wc -l` returns `0`
    - `rg "allowedDomains" skills/skills/SKILL.md` returns at least one match (gate logic references it)
    - `rg "allowWrite" skills/skills/SKILL.md` returns at least one match (gate logic references it)
    - `rg "BLOCK" skills/skills/SKILL.md` matches at least one line in Step 3 describing network/filesystem block conditions
    - `rg "WARN" skills/skills/SKILL.md` matches at least one line in Step 3 describing bins/env warn conditions
    - `rg "skills.sh.*not in your sandbox" skills/skills/SKILL.md` returns a match (D-06 exact warn text)
    - `rg "registry.npmjs.org" skills/skills/SKILL.md` returns a match (D-06 warn includes this domain)
    - skills/skills/SKILL.md does NOT contain `metadata.openclaw.requires`
    - Step 3 in the file contains the text "sandbox.network.allowedDomains"
    - Step 3 in the file contains the text "sandbox.filesystem.allowWrite"
  </acceptance_criteria>
  <done>Frontmatter has compatibility field. Step 2 has pre-install domain check with exact warn message. Step 3 is fully replaced: reads compatibility prose, classifies into bins/env/network/filesystem, BLOCKs on network/filesystem mismatches, WARNs on bins/env, displays audit table. Zero OpenShell references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add skill-doctor command section</name>
  <read_first>
    - skills/skills/SKILL.md — read after Task 1 completes to see current state, then find the ### update section to insert skill-doctor after it
    - .planning/phases/13-policy-gate-rework/13-CONTEXT.md — D-08 through D-10 locked decisions; exact table format from §Specifics; skill-doctor disk-scan discretion guidance
    - .planning/phases/13-policy-gate-rework/13-RESEARCH.md — skill-doctor command pattern, bash blocks for CLI ops, open question recommendation for compatibility-absent skills
  </read_first>
  <files>skills/skills/SKILL.md</files>
  <action>
Append a new `### skill-doctor` section after the `### update` section and before `## Error Handling`. The section must follow the same bash-block-for-CLI-ops pattern established throughout the file.

Insert the following after the `### update` section:

```markdown
### skill-doctor

Audit all installed skills against this agent's current sandbox configuration. Reports whether each skill's requirements are satisfied.

**Step 1: Discover installed skills**

Union two sources to build the complete skill list:

```bash
# Source 1: tracked skills
cat .claude/skills/installed.json

# Source 2: scan disk for any SKILL.md not in installed.json
ls .claude/skills/
```

Skills found on disk but absent from `installed.json` are labeled `manual` (matching the `list` command convention).

**Step 2: Read sandbox configuration**

```bash
cat .claude/settings.json
```

Extract `sandbox.network.allowedDomains` and `sandbox.filesystem.allowWrite` from the output.

**Step 3: Check each skill**

For each skill (from both sources):

```bash
# Read the skill's compatibility field
head -20 .claude/skills/<name>/SKILL.md
```

If the skill has no `compatibility` field: mark all checks as `—` (no requirements) and status as `PASS`.

If a `compatibility` field is present: interpret it to identify bins, env vars, network domains, and filesystem requirements (same logic as the install gate in Step 3 of `install`).

```bash
# Check each required binary
which <bin>

# Check each required env var
printenv <VAR>
```

Compare required network domains against `allowedDomains` and required filesystem write paths against `allowWrite`.

**Step 4: Report results**

Present a single table covering all skills:

| Skill | Source | Bins | Env Vars | Network | Status |
|-------|--------|------|----------|---------|--------|
| rightcron | skills.sh | — | — | — | PASS |
| my-skill | skills.sh | git ✓ | API_KEY ✗ | api.example.com ✗ | WARN |
| custom-tool | manual | docker ✓ | — | api.custom.com ✗ | BLOCK |

Status rules:
- **PASS**: all checks pass (or no requirements)
- **WARN**: one or more required binaries missing from PATH or env vars unset — but no sandbox violations
- **BLOCK**: one or more required network domains absent from `allowedDomains`, or required write path absent from `allowWrite`

After the table, summarize any actionable items:
- For each BLOCK: name the missing domain or write path and remind the user to update `agent.yaml` sandbox overrides and run `rightclaw up`.
- For each WARN: name the missing binary or env var and suggest installing/setting it.
```

Do not modify any other section.
  </action>
  <verify>
    <automated>
      rg "### skill-doctor" /home/wb/dev/rightclaw/skills/skills/SKILL.md
      rg "installed.json" /home/wb/dev/rightclaw/skills/skills/SKILL.md | wc -l
      rg "Source.*Bins.*Env Vars.*Network.*Status" /home/wb/dev/rightclaw/skills/skills/SKILL.md
      rg "PASS|WARN|BLOCK" /home/wb/dev/rightclaw/skills/skills/SKILL.md | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg "### skill-doctor" skills/skills/SKILL.md` returns exactly one match
    - `rg "installed.json" skills/skills/SKILL.md | wc -l` returns at least 2 (skill-doctor + step 4 of install)
    - The file contains a table header line with columns: `Skill`, `Source`, `Bins`, `Env Vars`, `Network`, `Status`
    - `rg "manual" skills/skills/SKILL.md` matches at least one line in the skill-doctor section (disk-only skills labeled manual)
    - The skill-doctor section contains `which <bin>` in a bash block
    - The skill-doctor section contains `printenv <VAR>` in a bash block
    - The skill-doctor section contains `cat .claude/settings.json` in a bash block
    - The skill-doctor section contains `cat .claude/skills/installed.json` in a bash block
    - The skill-doctor section contains `ls .claude/skills/` in a bash block (disk scan)
    - `rg "### skill-doctor" skills/skills/SKILL.md` appears BEFORE `## Error Handling` in the file (correct placement)
    - The section mentions `allowedDomains` and `allowWrite` when explaining BLOCK status
    - BLOCK, WARN, and PASS status rules are defined in the section
  </acceptance_criteria>
  <done>skill-doctor command section exists under ## Commands, after ### update, before ## Error Handling. Contains two-source discovery (installed.json + disk scan), sandbox config read, per-skill compatibility check, output table with all 6 columns (Skill/Source/Bins/Env Vars/Network/Status), and post-table summary of actionable items. PASS/WARN/BLOCK status rules clearly defined.</done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks against `skills/skills/SKILL.md`:

```bash
# GATE-01: No OpenShell references
rg "metadata.openclaw|policy.yaml|OpenShell" skills/skills/SKILL.md | wc -l
# Expected: 0

# GATE-02: settings.json field references present
rg "allowedDomains|allowWrite" skills/skills/SKILL.md
# Expected: multiple matches in Step 3 and skill-doctor

# D-07: compatibility frontmatter added
rg "^compatibility:" skills/skills/SKILL.md
# Expected: "compatibility: Requires Node.js (npx), internet access to skills.sh and npmjs.org"

# D-06: pre-install domain warn message present
rg "skills.sh.*not in your sandbox" skills/skills/SKILL.md
# Expected: one match

# skill-doctor command present
rg "### skill-doctor" skills/skills/SKILL.md
# Expected: one match

# Full file structural sanity
rg "^##|^###" skills/skills/SKILL.md
# Expected: ## When to Activate, ## Configuration, ## Commands, ### search, ### install, ### remove, ### list, ### update, ### skill-doctor, ## Error Handling, ## Important Rules
```
</verification>

<success_criteria>
1. `rg "metadata.openclaw|policy.yaml|OpenShell" skills/skills/SKILL.md | wc -l` returns `0` (GATE-01)
2. `rg "allowedDomains" skills/skills/SKILL.md` returns matches in Step 3 and skill-doctor (GATE-02)
3. `rg "^compatibility:" skills/skills/SKILL.md` returns the exact D-07 string
4. `rg "### skill-doctor" skills/skills/SKILL.md` returns one match
5. Step 3 BLOCKs on network/filesystem only; WARNs on bins/env only
6. Pre-install domain check (D-06) is in Step 2 with exact warn message from CONTEXT.md
7. skill-doctor table has all 6 columns and covers both installed.json and disk-only skills
</success_criteria>

<output>
After completion, create `.planning/phases/13-policy-gate-rework/13-01-SUMMARY.md` with:
- What changed in SKILL.md (sections modified/added)
- Verification results (grep outputs confirming GATE-01 and GATE-02)
- Any discretion choices made (prose interpretation logic, exact warn messages used)
</output>
```
