---
phase: 23-bot-skeleton
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/rightclaw/src/agent/types.rs
autonomous: true
requirements:
  - BOT-05
must_haves:
  truths:
    - "allowed_chat_ids field deserializes correctly from agent.yaml with Vec<i64> values"
    - "Absent allowed_chat_ids in agent.yaml yields empty Vec (serde default)"
    - "Existing agent.yaml files without the field still deserialize without error"
  artifacts:
    - path: "crates/rightclaw/src/agent/types.rs"
      provides: "AgentConfig with allowed_chat_ids: Vec<i64> field"
      contains: "allowed_chat_ids"
  key_links:
    - from: "crates/rightclaw/src/agent/types.rs"
      to: "crates/bot/src/telegram/filter.rs"
      via: "allowed_chat_ids field read at bot startup"
      pattern: "allowed_chat_ids"
---

<objective>
Add `allowed_chat_ids: Vec<i64>` to `AgentConfig` in the `rightclaw` crate.

Purpose: Downstream bot crate (Plan 02) reads this field to filter incoming Telegram messages. Must be backward compatible — existing agent.yaml files that omit this field must continue to parse without error.
Output: Modified `crates/rightclaw/src/agent/types.rs` with new field + test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-bot-skeleton/23-CONTEXT.md
</context>

<feature>
  <name>allowed_chat_ids field on AgentConfig</name>
  <files>crates/rightclaw/src/agent/types.rs</files>
  <behavior>
    - Test 1: `allowed_chat_ids: [123456789, -1001234567890]` deserializes to `Vec<i64>` with those two values
    - Test 2: YAML `{}` (no field) deserializes to `allowed_chat_ids == vec![]` (empty vec, not error)
    - Test 3: Existing full YAML without the field continues to deserialize — no unknown field rejection
    - Test 4: Negative i64 values (group chat IDs) deserialize correctly (e.g. `-1001234567890`)
    - Test 5: `agent_config_deserializes_minimal_yaml_with_defaults` still passes (regression guard)
    - Test 6: `agent_config_without_telegram_defaults_to_none` still passes (regression guard)
  </behavior>
  <implementation>
    Add the following field to `AgentConfig` struct in `crates/rightclaw/src/agent/types.rs`, after the `telegram_user_id` field:

    ```rust
    /// Telegram chat IDs permitted to interact with this agent's bot.
    /// Empty vec = block all incoming messages (secure default). Bot emits
    /// `tracing::warn!` at startup when empty — see D-05.
    #[serde(default)]
    pub allowed_chat_ids: Vec<i64>,
    ```

    No other changes to the struct. Field uses `#[serde(default)]` so it is backward compatible with existing agent.yaml files that lack it (required because `AgentConfig` has `#[serde(deny_unknown_fields)]`).

    Add tests inside the existing `#[cfg(test)]` module in the same file:

    - `agent_config_allowed_chat_ids_deserializes_list`: parse `allowed_chat_ids:\n  - 123456789\n  - -1001234567890` and assert `config.allowed_chat_ids == vec![123456789_i64, -1001234567890_i64]`
    - `agent_config_allowed_chat_ids_defaults_to_empty`: parse `"{}"` and assert `config.allowed_chat_ids.is_empty()`
    - `agent_config_allowed_chat_ids_absent_does_not_reject`: parse a YAML with other fields but no `allowed_chat_ids` and confirm `Ok`
    - `agent_config_allowed_chat_ids_negative_values`: parse `allowed_chat_ids:\n  - -100` and assert value is `-100_i64`
  </implementation>
</feature>

<verification>
```bash
cd /home/wb/dev/rightclaw && cargo test -p rightclaw agent_config -- --nocapture 2>&1 | tail -30
```

All `agent_config_*` tests pass, including pre-existing ones.
</verification>

<success_criteria>
- `allowed_chat_ids: Vec<i64>` field present in `AgentConfig` with `#[serde(default)]`
- All new tests pass: deserializes list, defaults to empty, negative values work
- All pre-existing `agent_config_*` tests still pass (backward compat maintained)
- `cargo test -p rightclaw` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/23-bot-skeleton/23-01-SUMMARY.md`
</output>
