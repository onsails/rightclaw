---
status: complete
phase: 28.1-v3-0-uat-manual-end-to-end-testing-of-teloxide-bot-cron-runtime-and-rightclaw-up-flow
source: [26-01-SUMMARY.md, 26-02-SUMMARY.md, 25-03-SUMMARY.md, 25.5-02-SUMMARY.md, 24-03-SUMMARY.md, 27-01-SUMMARY.md, 27-02-SUMMARY.md, 28-01-SUMMARY.md]
started: 2026-04-01T00:00:00Z
updated: 2026-04-01T22:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running rightclaw/process-compose instances. Clear any leftover lock files or temp state. Run `rightclaw up` from scratch. The command should boot without errors: process-compose starts, bot processes appear in the TUI/log, and no panics or fatal errors are printed.
result: issue
reported: "restart loop — deleteWebhook failed — long polling would compete with active webhook: A network error: error sending request"
severity: blocker

### 2. Bot-Only Process-Compose Generation
expected: After `rightclaw up`, inspect the generated process-compose.yaml (usually at `~/.rightclaw/run/<agent>/process-compose.yaml` or similar). It should have `<agent-name>-bot:` process keys with command `rightclaw bot --agent <name>`. There should be NO `--channels plugin:telegram` flag and no `is_interactive` field.
result: pass

### 3. System Prompt Composition (via Agent Definition)
expected: After `rightclaw up`, check `.claude/agents/<name>.md` in the agent dir. It should contain a YAML-frontmatter agent definition with identity content from SOUL.md/AGENTS.md composited in. Note: Phase 25.5 replaced system-prompt.txt with this agent definition file approach.
result: pass
note: system-prompt.txt superseded by .claude/agents/<name>.md in Phase 25.5 — file exists with composed identity content

### 4. Agent Definition Files Generated
expected: After `rightclaw up`, `.claude/agents/<name>.md` and `.claude/reply-schema.json` exist in the agent dir. Generated by rightclaw from identity files.
result: pass

### 5. Telegram Bot Receives Message and Replies
expected: Send a plain text message to the agent's Telegram bot. The bot should receive the message, dispatch it to Claude Code via `--agent <name>`, and send a reply back to Telegram.
result: blocked
blocked_by: prior-phase
reason: "Bot in restart loop due to Test 1 blocker — bot cannot start until native-tls added to teloxide"

### 6. Telegram Session Continuity
expected: Send two follow-up messages to the same bot in the same thread. Second message answered in context of first (CC session reused).
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 — bot cannot start"

### 7. Telegram /reset Command
expected: Send `/reset` to the bot, get confirmation reply, subsequent messages start fresh CC session.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 — bot cannot start"

### 8. Cron Runtime Starts With Bot
expected: Bot logs show cron task started alongside Telegram dispatcher on launch.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 — bot cannot start"

### 9. Cron Job Fires on Schedule
expected: Create crons/*.yaml with `schedule: "* * * * *"`, wait for fire — log appears at crons/logs/, cron_runs row inserted with status success/failed.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 — bot cannot start (though cron_runs table verified present in DB, user_version=3)"

### 10. Cron Lock Prevents Duplicate Runs
expected: Lock file prevents second subprocess when first is still running. Logs show "lock is fresh, skipping" type message.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 — bot cannot start"

### 11. MCP cron_list_runs Tool
expected: Via rightclaw MCP server, call `cron_list_runs` — returns JSON list from cron_runs table with job_name, status, started_at, finished_at, log_path.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 — bot cannot start; MCP server only starts with bot"

### 12. MCP cron_show_run Tool
expected: Call `cron_show_run` with a run_id — returns full row. Non-existent ID returns "not found" message (not MCP error).
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 1 — bot cannot start"

### 13. rightclaw doctor Webhook Check
expected: `rightclaw doctor` warns about active webhooks for agents with Telegram tokens. Passes silently when no webhook active.
result: issue
reported: "rightclaw doctor panics: Cannot start a runtime from within a runtime"
severity: major

### 14. cronsync Skill Creates Cron File
expected: `/rightcron` skill creates `crons/<job-name>.yaml` with schedule and prompt fields. Skill does NOT execute the job — file management only.
result: blocked
blocked_by: prior-phase
reason: "Requires running agent — blocked by Test 1"

## Summary

total: 14
passed: 3
issues: 2
pending: 0
skipped: 0
blocked: 9

## Gaps

- truth: "rightclaw up boots without errors — bot process starts and stays running"
  status: resolved
  reason: "User reported: restart loop — deleteWebhook failed — long polling would compete with active webhook: A network error: error sending request"
  severity: blocker
  test: 1
  root_cause: "teloxide dependency has default-features=false which strips native-tls from teloxide-core's reqwest 0.12. No TLS support → every https://api.telegram.org call fails with 'error sending request'. Fix: add native-tls to teloxide features in workspace Cargo.toml."
  resolved_by: "phase 28.2 plan 28.2-01 — added rustls feature to teloxide workspace dep (rustls preferred over native-tls: pure-Rust, no OpenSSL dependency)"
  artifacts:
    - path: "Cargo.toml"
      issue: "teloxide = { version = \"0.17\", default-features = false, features = [\"macros\", \"throttle\", \"cache-me\"] } — missing native-tls feature"
  debug_session: ""

- truth: "rightclaw doctor completes without panic and shows webhook check results"
  status: resolved
  reason: "User reported: rightclaw doctor panics — Cannot start a runtime from within a runtime"
  severity: major
  test: 13
  root_cause: "fetch_webhook_url in doctor.rs calls tokio::runtime::Runtime::new().block_on() inside existing #[tokio::main] async context — nested runtimes panic in tokio. Fix: replace with tokio::task::block_in_place or make fetch_webhook_url async."
  resolved_by: "phase 28.2 plan 28.2-02 — replaced Runtime::new().block_on() with tokio::task::block_in_place pattern; regression test added"
  artifacts:
    - path: "crates/rightclaw/src/doctor.rs"
      issue: "fetch_webhook_url at line ~431 creates Runtime::new().block_on() inside async context"
  debug_session: ""
