---
status: complete
phase: v3.2-mcp-oauth-uat
source: [VERIFICATION.md, v3.2-gaps-PLAN.md]
started: 2026-04-05T18:00:00Z
updated: 2026-04-05T19:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. rightclaw mcp status CLI
expected: Run `rightclaw mcp status`. Should print per-agent MCP auth status table with server names and ✅/❌/⚠️ icons. If no HTTP MCP servers configured, prints "No HTTP MCP servers configured."
result: pass

### 2. rightclaw mcp status --agent filter
expected: Run `rightclaw mcp status --agent <name>` with a valid agent name. Should show only that agent's MCP servers. With a nonexistent agent name, should error "agent not found: <name>".
result: pass

### 3. /mcp add via Telegram
expected: Send `/mcp add notion https://mcp.notion.com/mcp` in Telegram. Bot should add the server entry to agent's .mcp.json and confirm — NOT launch `claude -p`.
result: pass

### 4. /mcp list via Telegram
expected: Send `/mcp list` in Telegram. Response shows lines like `✅ notion  —  present` with emoji icon, server name, em-dash, and AuthState label per server.
result: pass

### 5. /mcp remove via Telegram
expected: Send `/mcp remove notion` in Telegram. Bot removes the server from .mcp.json and confirms.
result: pass

### 6. /doctor via Telegram
expected: Send `/doctor` in Telegram. Doctor output appears in a monospace code block (visually distinct from plain text — HTML `<pre>` tags).
result: pass
note: Required HTML-escape fix for doctor output containing special characters. Added tracing + plain-text fallback on HTML rejection.

### 7. Bot startup MCP auth warnings
expected: Start bot with an agent that has an unauthenticated HTTP MCP server in .mcp.json. Logs should contain `WARN MCP server needs auth server=<name> state=auth required` between deleteWebhook and cron spawn.
result: pass

### 8. cloudflared-start.sh runtime content
expected: Run `rightclaw up` with tunnel configured, then read `~/.rightclaw/scripts/cloudflared-start.sh`. Exec line should contain `--config` flag pointing to cloudflared-config.yml.
result: pass

### 9. /mcp auth OAuth flow via Telegram
expected: Send `/mcp auth notion` in Telegram. Bot should initiate OAuth flow — discover AS metadata, generate PKCE + state, respond with authorization URL.
result: blocked
blocked_by: third-party
reason: "OAuth discovery works, but cloudflared tunnel returns 530 — DNS record conflict for right.onsails.me (existing A/AAAA record blocks CNAME to tunnel). Infrastructure issue, not code bug."

## Summary

total: 9
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

