---
status: partial
phase: v3.2-mcp-oauth
source: [32-01-SUMMARY.md, 33-01-SUMMARY.md, 34-01-SUMMARY.md, 34-02-SUMMARY.md, 34-03-SUMMARY.md, 34-04-SUMMARY.md, 35-01-SUMMARY.md, 35-02-SUMMARY.md, 35-03-SUMMARY.md]
started: 2026-04-04T00:00:00Z
updated: 2026-04-04T10:00:00Z
---

## Current Test

[testing paused — tunnel setup gap must be fixed before tests 10-11 can proceed]

## Tests

### 1. rightclaw init --tunnel-token saves config
expected: Run `rightclaw init --tunnel-token <some-token>`. Command succeeds. Inspect `~/.rightclaw/config.yaml` — it should contain only `tunnel.token` (no URL field — hostname is derived from JWT at runtime). Running `rightclaw init` again with a different token should overwrite it.
result: pass
notes: Fixed CF token format (single-segment base64 JSON, not JWT). Init succeeded after fix.

### 2. rightclaw doctor — cloudflared binary check
expected: Run `rightclaw doctor`. If cloudflared is not installed, a Warn entry appears: "cloudflared binary not found" with an install suggestion. If it is installed, the check shows Pass.
result: pass

### 3. rightclaw doctor — tunnel config check
expected: Run `rightclaw doctor`. If no tunnel has been configured (no `rightclaw init --tunnel-token`), a Warn entry appears: "tunnel not configured" with `rightclaw init --tunnel-token` as the fix command. After running init with a token, re-running doctor shows Pass.
result: pass

### 4. rightclaw doctor — mcp-tokens check
expected: Run `rightclaw doctor`. A "mcp-tokens" check is present. If all agent tokens are valid (or non-expiring), it shows Pass. If any token is expired or missing, it shows Warn listing the agent/server pair. Tokens with expires_at=0 are treated as non-expiring (Pass).
result: pass

### 5. Telegram /mcp add and /mcp remove
expected: Send `/mcp add notion https://mcp.notion.com/mcp`. Bot confirms server added. Send `rightclaw mcp status` and the server appears as MISSING. Then send `/mcp remove notion` — bot confirms removal, server disappears from status.
result: pass
notes: Fixed dptree Arc<PathBuf> TypeId collision (agent_dir/home newtypes). mcp status shows 'missing' label (minor UX issue logged separately). Remove confirmed working.

### 6. rightclaw mcp status (all agents)
expected: Run `rightclaw mcp status`. Should print a table grouped by agent showing each HTTP MCP server with its auth state: OK (token present + valid), MISSING (no token), or EXPIRED (token past expires_at). Stdio-only agents produce no output (skipped silently).
result: pass

### 7. rightclaw mcp status --agent <name>
expected: Run `rightclaw mcp status --agent <some-agent>`. Output filtered to that single agent only. Unknown agent name returns an error.
result: pass

### 8. rightclaw up warns about unauthenticated servers
expected: Run `rightclaw up` with at least one agent that has an HTTP MCP server with no token. Before process-compose launches, a warning line is logged naming the agent/server pairs that are MISSING or EXPIRED (e.g. "warn: agent-foo/notion token missing").
result: issue
reported: "Warning IS generated via tracing::warn! in cmd_up but invisible — TUI launches immediately and overwrites stdout. Also bot doesn't log MCP token status at startup at all."
severity: minor

### 9. Telegram /mcp list
expected: Send `/mcp list` in Telegram. Bot replies with a formatted table showing each MCP server per agent with icon: ✅ (OK), ❌ (MISSING), ⚠️ (EXPIRED). If no HTTP MCP servers configured, bot replies gracefully.
result: pass
notes: "MISSING" label shown instead of something more informative — tracked in gaps.

### 10. Telegram /mcp auth — OAuth flow initiation
expected: Send `/mcp auth notion` (or any MISSING server). Bot responds with an OAuth authorization URL to open in browser. URL contains PKCE challenge parameters. After opening, cloudflared tunnel routes the callback to the bot. (Requires cloudflared tunnel configured and running.)
result: issue
reported: "Discovering OAuth endpoints for notion... Tunnel healthcheck failed: error sending request for url (https://aaaabbbb-0000-1111-2222-ccccddddeeee.cfargotunnel.com/) — Is cloudflared running?"
severity: major

### 11. Telegram /doctor
expected: Send `/doctor` in Telegram. Bot replies with a formatted summary of doctor checks including the cloudflared and mcp-tokens checks. Pass count shown. Checks match `rightclaw doctor` CLI output.
result: [pending]

## Summary

total: 11
passed: 0
issues: 0
passed: 8
issues: 5
pending: 1
skipped: 0

## Gaps

- truth: "MCP command handling is logged in bot output"
  status: failed
  reason: "User reported: nothing in rightclaw logs after 'message update received by dispatcher' — no tracing in mcp handlers makes debugging impossible"
  severity: minor
  test: 5
  root_cause: "handle_mcp, handle_mcp_add, handle_mcp_remove, handle_mcp_list have no tracing::info! calls"
  artifacts:
    - path: "crates/bot/src/telegram/handler.rs"
      issue: "no tracing in mcp command handlers"
  missing:
    - "Add tracing::info! at entry of each mcp subcommand handler with command + args"

- truth: "rightclaw up MCP warning is visible to user before TUI launches"
  status: failed
  reason: "User reported: warning generated via tracing::warn! in cmd_up but invisible — TUI takes over stdout immediately. Bot also doesn't log MCP token status at startup."
  severity: minor
  test: 8
  root_cause: "tracing output lost to TUI. No persistent log file for pre-launch output. Bot has no MCP status check at startup."
  artifacts:
    - path: "crates/rightclaw-cli/src/main.rs"
      issue: "MCP warning logged via tracing::warn! but swallowed by TUI — needs eprintln! or log file"
    - path: "crates/bot/src/lib.rs"
      issue: "no MCP token validation at bot startup"
  missing:
    - "Use eprintln! for MCP warning before process-compose launch (always visible)"
    - "Add MCP token status check in bot lib.rs startup with tracing::warn! per missing server"

- truth: "rightclaw mcp status shows clear auth state labels"
  status: failed
  reason: "User reported: 'missing' label is uninformative — unclear if it means no server config or no token"
  severity: minor
  test: 5
  root_cause: "State label 'missing' doesn't convey that server IS configured but auth token is absent"
  artifacts:
    - path: "crates/rightclaw/src/mcp/detect.rs"
      issue: "auth state label 'missing' should be 'no token' or 'auth required'"
  missing:
    - "Rename MISSING state display to 'no token' or 'auth required'"

- truth: "OAuth flow initiates successfully via /mcp auth"
  status: failed
  reason: "User reported: 'Tunnel healthcheck failed: error sending request for url (https://aaaabbbb-0000-1111-2222-ccccddddeeee.cfargotunnel.com/)' — UUID hostname not publicly routable without DNS. Full tunnel setup flow is broken."
  severity: major
  test: 10
  root_cause: |
    UUID-based cfargotunnel.com URL requires DNS routing (CNAME or cloudflared route dns). Not automatic.
    Current flow: (1) no --tunnel-hostname in init, (2) cloudflared starts without routing DNS first, (3) healthcheck fails on unrouted UUID URL.
    Required flow: (1) init requires --tunnel-hostname <domain>, (2) cloudflared wrapper script runs `cloudflared tunnel route dns <uuid> <hostname>` THEN starts tunnel, (3) doctor reports if routing failed or token invalid.
  artifacts:
    - path: "crates/rightclaw-cli/src/main.rs"
      issue: "rightclaw init missing --tunnel-hostname flag (separate from --tunnel-token)"
    - path: "crates/rightclaw/src/config.rs"
      issue: "GlobalConfig.TunnelConfig missing hostname field"
    - path: "crates/rightclaw/src/codegen/cloudflared.rs"
      issue: "generates process-compose cloudflared entry without dns route step"
    - path: "crates/bot/src/telegram/handler.rs"
      issue: "healthcheck uses UUID URL instead of configured tunnel hostname"
  missing:
    - "Add --tunnel-hostname <domain> to rightclaw init (required, validate it's a domain not URL)"
    - "Store tunnel.hostname in config.yaml alongside tunnel.token"
    - "Generate cloudflared wrapper script: runs `cloudflared tunnel route dns <uuid> <hostname>` first, then `cloudflared tunnel run --token ...`"
    - "process-compose cloudflared entry uses wrapper script"
    - "doctor checks: tunnel token valid, route dns succeeded, cloudflared process alive — reports specific errors"
    - "Bot healthcheck uses tunnel.hostname from config instead of UUID-derived hostname"

- truth: "Bot /mcp add writes to agent .mcp.json and rightclaw mcp status reflects it"
  status: failed
  reason: "User reported: Bot replied 'Added MCP server: notion' but mcp status shows 'No MCP OAuth servers found'. notion entry missing from ~/.rightclaw/agents/right/.mcp.json. Also only /reset registered as bot command."
  severity: major
  test: 5
  root_cause: "Both agent_dir and home are Arc<PathBuf> — same TypeId. dptree resolves deps by type, so home_arc (registered last) overwrites agent_dir_arc. All handlers receive ~/.rightclaw for both. handle_mcp_add writes to ~/.rightclaw/.mcp.json instead of ~/.rightclaw/agents/right/.mcp.json."
  artifacts:
    - path: "crates/bot/src/telegram/dispatch.rs"
      issue: "agent_dir_arc and home_arc both typed Arc<PathBuf> — dptree TypeId collision"
  missing:
    - "Wrap in newtypes: AgentDir(PathBuf) and RightclawHome(PathBuf) so dptree can distinguish them"
