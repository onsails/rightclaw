---
phase: 14-rightskills-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/rightskills/SKILL.md
  - crates/rightclaw/src/codegen/skills.rs
  - crates/rightclaw/src/init.rs
autonomous: true
requirements:
  - RS-01
  - RS-02
  - RS-03
  - RS-04

must_haves:
  truths:
    - "Invoking `/rightskills` activates the skill manager (SKILL.md name field is `rightskills`)"
    - "The heading in SKILL.md reads `/rightskills -- Agent Skills Manager`"
    - "The source skill directory on disk is `skills/rightskills/` — `skills/skills/` does not exist"
    - "`rightclaw` compiles cleanly after the rename (include_str! path resolves)"
    - "Running `rightclaw init` produces `agents/right/.claude/skills/rightskills/SKILL.md` (not `skills/SKILL.md`)"
    - "All tests pass — no assertion references `skills/skills/` or `.claude/skills/skills/`"
  artifacts:
    - path: "skills/rightskills/SKILL.md"
      provides: "Renamed skill source with updated name field and heading"
      contains: "name: rightskills"
    - path: "crates/rightclaw/src/codegen/skills.rs"
      provides: "Updated constant, include path, install path, test assertions"
      exports: ["SKILL_RIGHTSKILLS", "install_builtin_skills"]
    - path: "crates/rightclaw/src/init.rs"
      provides: "Updated println and test assertion for new install path"
  key_links:
    - from: "crates/rightclaw/src/codegen/skills.rs"
      to: "skills/rightskills/SKILL.md"
      via: "include_str! macro at compile time"
      pattern: "include_str!.*skills/rightskills/SKILL.md"
    - from: "crates/rightclaw/src/codegen/skills.rs"
      to: ".claude/skills/rightskills/SKILL.md"
      via: "install_builtin_skills() path tuple"
      pattern: "rightskills/SKILL.md"
---

<objective>
Rename the `/skills` built-in skill to `/rightskills` across all touch points: the source directory on disk, the SKILL.md frontmatter and heading, the Rust constant and include path, the install path tuple, and all test assertions in `skills.rs` and `init.rs`.

Purpose: Agents invoke the skill manager as `/rightskills` instead of `/skills`, avoiding collision with the generic word "skills" and matching the RightClaw brand.
Output: Renamed directory at `skills/rightskills/`, updated SKILL.md, updated Rust code, all tests green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-rightskills-rename/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename skill directory and update SKILL.md content</name>
  <files>skills/rightskills/SKILL.md</files>

  <read_first>
    - skills/skills/SKILL.md — full current content; must read before any edit
    - .planning/phases/14-rightskills-rename/14-CONTEXT.md — D-01, D-07, D-08 decisions
  </read_first>

  <action>
Rename the source directory using `git mv` so git history is preserved:

```bash
git mv skills/skills skills/rightskills
```

Then edit `skills/rightskills/SKILL.md` (the file at its new location after the rename). Make exactly two changes — nothing else in the file should be touched:

1. **Frontmatter `name` field** (line 1, per D-07):
   - Old: `name: skills`
   - New: `name: rightskills`

2. **H1 heading** (line 12, per D-08 — this is the slash command invocation name):
   - Old: `# /skills -- Agent Skills Manager (skills.sh)`
   - New: `# /rightskills -- Agent Skills Manager (skills.sh)`

**Do NOT change** any of the following — they are install paths or domain names, not slash command invocations:
- `.claude/skills/<name>/` occurrences (these are agent-side install paths, correct as-is)
- `skills.sh` URLs (domain name, not a slash command)
- `npx skills` CLI commands (the npm package name, not our slash command)
- The `### skill-doctor` section heading (per D-09, unchanged)
- Any prose using the word "skills" not preceded by a backtick-slash

Verify the git rename succeeded and the file is tracked under the new path:
```bash
git status
```
  </action>

  <verify>
    <automated>
      rg "^name: rightskills" /home/wb/dev/rightclaw/skills/rightskills/SKILL.md
      rg "^# /rightskills" /home/wb/dev/rightclaw/skills/rightskills/SKILL.md
      test ! -d /home/wb/dev/rightclaw/skills/skills
    </automated>
  </verify>

  <acceptance_criteria>
    - `skills/rightskills/SKILL.md` exists on disk
    - `skills/skills/` directory does NOT exist
    - `rg "^name: rightskills" skills/rightskills/SKILL.md` exits 0
    - `rg "^# /rightskills" skills/rightskills/SKILL.md` exits 0
    - `rg "^name: skills$" skills/rightskills/SKILL.md` exits non-zero (old name gone)
    - `rg "^# /skills" skills/rightskills/SKILL.md` exits non-zero (old heading gone)
    - No occurrences of `name: skills` remain at line 1 of the file
    - `git status` shows the rename (not an untracked add + delete)
  </acceptance_criteria>

  <done>Directory renamed to `skills/rightskills/`, SKILL.md frontmatter says `name: rightskills`, heading reads `# /rightskills -- Agent Skills Manager (skills.sh)`. All other content unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Rust constant, include path, install path, and test assertions</name>
  <files>crates/rightclaw/src/codegen/skills.rs, crates/rightclaw/src/init.rs</files>

  <read_first>
    - crates/rightclaw/src/codegen/skills.rs — full current content; must read before any edit
    - crates/rightclaw/src/init.rs — full current content; must read before any edit
    - .planning/phases/14-rightskills-rename/14-CONTEXT.md — D-02, D-03, D-04, D-05, D-06
  </read_first>

  <behavior>
    - Test `installs_skills_skill`: asserts `.claude/skills/rightskills/SKILL.md` exists (was `skills/SKILL.md`)
    - Test `install_is_idempotent`: asserts `.claude/skills/rightskills/SKILL.md` exists (was `skills/SKILL.md`)
    - Test `init_creates_default_agent_files` in `init.rs`: asserts `.claude/skills/rightskills/SKILL.md` exists (was `skills/SKILL.md`)
    - All above tests must FAIL before the Rust changes, PASS after
  </behavior>

  <action>
Edit `crates/rightclaw/src/codegen/skills.rs`. Make these exact changes:

1. **Constant rename** (D-02):
   - Old: `const SKILL_SKILLS: &str = include_str!("../../../../skills/skills/SKILL.md");`
   - New: `const SKILL_RIGHTSKILLS: &str = include_str!("../../../../skills/rightskills/SKILL.md");`

2. **Doc comment update** (line 8 — keep accurate):
   - Old: `/// Writes \`skills/SKILL.md\`, \`rightcron/SKILL.md\`, and \`installed.json\`.`
   - New: `/// Writes \`rightskills/SKILL.md\`, \`rightcron/SKILL.md\`, and \`installed.json\`.`

3. **Install path tuple** (D-04) — the path used to write into `.claude/skills/`:
   - Old: `("skills/SKILL.md", SKILL_SKILLS),`
   - New: `("rightskills/SKILL.md", SKILL_RIGHTSKILLS),`

4. **Test assertions** (D-05) — update all occurrences of `skills/SKILL.md` in the test module to `rightskills/SKILL.md`:
   - In `installs_skills_skill`: `".claude/skills/skills/SKILL.md"` → `".claude/skills/rightskills/SKILL.md"`, message `"skills/SKILL.md should exist"` → `"rightskills/SKILL.md should exist"`
   - In `install_is_idempotent`: `".claude/skills/skills/SKILL.md"` → `".claude/skills/rightskills/SKILL.md"`

Edit `crates/rightclaw/src/init.rs`. Make these exact changes:

5. **println display line** (D-06, line 173):
   - Old: `println!("  agents/right/.claude/skills/skills/SKILL.md  (skills.sh manager)");`
   - New: `println!("  agents/right/.claude/skills/rightskills/SKILL.md  (skills.sh manager)");`

6. **Test assertion in `init_creates_default_agent_files`** (D-05/D-06, line 250-252):
   - Old: `agents_dir.join(".claude/skills/skills/SKILL.md").exists()`, message `"skills skill should be installed"`
   - New: `agents_dir.join(".claude/skills/rightskills/SKILL.md").exists()`, message `"rightskills skill should be installed"`

After edits, build the full workspace to confirm the `include_str!` resolves (Task 1's directory rename must be complete first):
```bash
cargo build --workspace
```

Then run all tests:
```bash
cargo test --workspace
```
  </action>

  <verify>
    <automated>cargo test --workspace 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `rg "SKILL_RIGHTSKILLS" crates/rightclaw/src/codegen/skills.rs` exits 0
    - `rg "SKILL_SKILLS[^H]" crates/rightclaw/src/codegen/skills.rs` exits non-zero (old constant gone; `[^H]` avoids matching `SKILL_RIGHTSKILLS`)
    - `rg "skills/rightskills/SKILL.md" crates/rightclaw/src/codegen/skills.rs` exits 0 (include_str path)
    - `rg "skills/skills/SKILL.md" crates/rightclaw/src/codegen/skills.rs` exits non-zero (old path gone)
    - `rg "rightskills/SKILL.md.*SKILL_RIGHTSKILLS" crates/rightclaw/src/codegen/skills.rs` exits 0 (install tuple updated)
    - `rg "rightskills/SKILL.md" crates/rightclaw/src/init.rs` exits 0 (println updated)
    - `rg "skills/skills/SKILL.md" crates/rightclaw/src/init.rs` exits non-zero (old path gone in init.rs)
    - `cargo build --workspace` exits 0
    - `cargo test --workspace` exits 0 with all tests passing
    - No test output contains `FAILED`
  </acceptance_criteria>

  <done>Rust constant is `SKILL_RIGHTSKILLS`, `include_str!` points to `skills/rightskills/SKILL.md`, install path tuple writes to `.claude/skills/rightskills/SKILL.md`, all test assertions updated, workspace builds and all tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full rename end-to-end:

```bash
# 1. Old directory gone
test ! -d /home/wb/dev/rightclaw/skills/skills && echo "PASS: old dir gone"

# 2. New directory and SKILL.md present with correct name field
rg "^name: rightskills" /home/wb/dev/rightclaw/skills/rightskills/SKILL.md && echo "PASS: name field correct"

# 3. No stale references to old paths anywhere in the codebase
rg "skills/skills" /home/wb/dev/rightclaw/crates/ && echo "WARN: stale references found" || echo "PASS: no stale refs in crates/"

# 4. Workspace compiles
cargo build --workspace --quiet && echo "PASS: build clean"

# 5. All tests green
cargo test --workspace --quiet 2>&1 | tail -5
```
</verification>

<success_criteria>
1. `skills/rightskills/SKILL.md` exists; `skills/skills/` does not
2. SKILL.md frontmatter: `name: rightskills`; heading: `# /rightskills -- Agent Skills Manager (skills.sh)`
3. `crates/rightclaw/src/codegen/skills.rs` uses `SKILL_RIGHTSKILLS`, `include_str!("../../../../skills/rightskills/SKILL.md")`, and install path `"rightskills/SKILL.md"`
4. `crates/rightclaw/src/init.rs` println and test assertion reference `.claude/skills/rightskills/SKILL.md`
5. `cargo build --workspace` and `cargo test --workspace` both exit 0 with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/14-rightskills-rename/14-01-SUMMARY.md` following the summary template.
</output>
