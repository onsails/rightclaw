---
phase: 03-default-agent-and-installation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/right/BOOTSTRAP.md
  - templates/right/policy.yaml
  - templates/right/policy-telegram.yaml
autonomous: true
requirements: [DFLT-02, DFLT-03, DFLT-04, SAND-04, SAND-05, CHAN-03]

must_haves:
  truths:
    - "BOOTSTRAP.md asks 4 onboarding questions: name, creature type, vibe, emoji"
    - "BOOTSTRAP.md instructs agent to write IDENTITY.md, USER.md, SOUL.md then self-delete"
    - "policy.yaml uses hard_requirement for Landlock"
    - "policy.yaml covers filesystem, network, and process restrictions with comprehensive comments"
    - "policy-telegram.yaml has api.telegram.org uncommented in network_policies"
    - "policy.yaml has api.telegram.org commented out in network_policies"
  artifacts:
    - path: "templates/right/BOOTSTRAP.md"
      provides: "Conversational onboarding template for first-run"
      contains: "IDENTITY.md"
    - path: "templates/right/policy.yaml"
      provides: "Production OpenShell policy without Telegram"
      contains: "hard_requirement"
    - path: "templates/right/policy-telegram.yaml"
      provides: "Production OpenShell policy with Telegram enabled"
      contains: "api.telegram.org"
  key_links:
    - from: "templates/right/BOOTSTRAP.md"
      to: "templates/right/IDENTITY.md"
      via: "agent writes IDENTITY.md during onboarding"
      pattern: "IDENTITY.md"
    - from: "templates/right/policy-telegram.yaml"
      to: "templates/right/policy.yaml"
      via: "identical except Telegram network rule is uncommented"
      pattern: "telegram"
---

<objective>
Create production-quality template files for the default "Right" agent: BOOTSTRAP.md onboarding conversation, and two policy.yaml variants (with/without Telegram network access).

Purpose: These templates are embedded at compile time via include_str! and copied to the user's agent directory during `rightclaw init`. They define the first-run experience and sandbox security boundary.

Output: Three template files in templates/right/ ready for include_str! embedding in Plan 03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-default-agent-and-installation/03-CONTEXT.md
@.planning/phases/03-default-agent-and-installation/03-RESEARCH.md
@templates/right/IDENTITY.md
@templates/right/SOUL.md

<interfaces>
<!-- Existing template files that BOOTSTRAP.md references -->

From templates/right/IDENTITY.md:
```markdown
You are Right -- a general-purpose personal AI agent running inside a RightClaw sandbox.
## Who you are
## Key principles
## How you work
## Self-configuration
```

From templates/right/SOUL.md:
```markdown
## Tone & Style
## Personality
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BOOTSTRAP.md onboarding template</name>
  <files>templates/right/BOOTSTRAP.md</files>
  <read_first>
    - templates/right/IDENTITY.md
    - templates/right/SOUL.md
    - .planning/phases/03-default-agent-and-installation/03-RESEARCH.md (BOOTSTRAP.md Template section)
    - .planning/phases/03-default-agent-and-installation/03-CONTEXT.md (D-01 through D-04)
  </read_first>
  <action>
Create `templates/right/BOOTSTRAP.md` as a conversational onboarding template.

Per D-01: The conversation asks exactly 4 questions:
1. **Name** -- what should the agent be called (Right is default, suggest alternatives)
2. **Creature type** -- what kind of being (AI assistant, familiar, daemon, ghost in the machine, etc.)
3. **Vibe** -- formal, casual, snarky, warm, chaotic, etc.
4. **Emoji** -- signature emoji, offer suggestions if stuck

Per D-02: This is a system prompt that Claude Code reads. The agent drives the conversation naturally -- NOT a form, NOT an interrogation. Conversational tone, suggest options when user seems stuck.

Per D-03: After collecting answers, the agent must:
- Rewrite **IDENTITY.md** with the chosen name, creature type, and vibe
- Create **USER.md** with the user's name, preferences, how to address them
- Rewrite **SOUL.md** with personality adapted to the chosen vibe

Per D-04: After writing all files, the agent must delete BOOTSTRAP.md itself (the file containing this prompt).

Structure the file with:
- A YAML frontmatter block with `summary: "First-run onboarding for Right agent"`
- An opening section that sets the scene -- the agent just came online, blank slate
- "The Conversation" section with the 4 questions, conversational guidance
- "After You Know Who You Are" section listing the 3 files to write/update
- "When You're Done" section instructing self-deletion of BOOTSTRAP.md
- Closing line for flavor

Use the OpenClaw-style template from 03-RESEARCH.md as a starting point but adapt it for RightClaw's identity.
  </action>
  <verify>
    <automated>test -f templates/right/BOOTSTRAP.md && grep -q "IDENTITY.md" templates/right/BOOTSTRAP.md && grep -q "USER.md" templates/right/BOOTSTRAP.md && grep -q "SOUL.md" templates/right/BOOTSTRAP.md && grep -q "Delete this file" templates/right/BOOTSTRAP.md && grep -q "emoji" templates/right/BOOTSTRAP.md && grep -q "vibe" templates/right/BOOTSTRAP.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at templates/right/BOOTSTRAP.md
    - Contains YAML frontmatter with `summary:`
    - Mentions all 4 onboarding questions: name, creature type, vibe, emoji
    - References writing IDENTITY.md, USER.md, SOUL.md
    - Instructs agent to delete BOOTSTRAP.md after completion
    - Conversational tone, not form-like
  </acceptance_criteria>
  <done>BOOTSTRAP.md template exists with 4-question onboarding flow, file-writing instructions, and self-delete directive</done>
</task>

<task type="auto">
  <name>Task 2: Create production policy.yaml and policy-telegram.yaml</name>
  <files>templates/right/policy.yaml, templates/right/policy-telegram.yaml</files>
  <read_first>
    - templates/right/policy.yaml (existing placeholder to overwrite)
    - .planning/phases/03-default-agent-and-installation/03-RESEARCH.md (OpenShell Policy YAML section)
    - .planning/phases/03-default-agent-and-installation/03-CONTEXT.md (D-11 through D-15)
  </read_first>
  <action>
**Overwrite** `templates/right/policy.yaml` (currently a placeholder) with a production-quality OpenShell policy.

Per D-11: Strict least-privilege -- agent dir read-write (via include_workdir), system paths read-only.
Per D-12: Network allows only api.github.com. Telegram rule is COMMENTED OUT in this variant.
Per D-13: `hard_requirement` for Landlock -- no silent degradation.
Per D-14: Comprehensive comments throughout showing HOW TO expand permissions:
  - How to allow all outbound network (with security warning)
  - How to add read-only access to a specific directory
  - How to add read-write access to a specific directory
  - How to give broad filesystem access (with security warning)
  - How to add additional network endpoints
  - How to allow npm/pip package installation
Per D-15: The file IS the documentation -- users learn OpenShell policy format from reading it.

Use the exact policy structure from the research document's "OpenShell Policy YAML (Production Quality)" section. Key sections:

```yaml
version: 1

filesystem_policy:
  include_workdir: true
  read_only:
    - /usr
    - /lib
    - /lib64
    - /etc
    - /proc
    - /dev/urandom
    - /dev/null
  read_write:
    - /tmp

landlock:
  compatibility: hard_requirement

process:
  run_as_user: sandbox
  run_as_group: sandbox

network_policies:
  github_api:
    name: github-api
    endpoints:
      - host: api.github.com
        port: 443
        protocol: rest
        tls: terminate
        enforcement: enforce
        access: read-only
    binaries:
      - { path: /usr/local/bin/claude }
      - { path: /usr/bin/curl }
      - { path: /usr/bin/gh }

  # -- Telegram (uncomment if you set up a Telegram bot) --
  # telegram_api:
  #   name: telegram-bot-api
  #   ... (commented out)
```

Also add read_only entries for paths needed by Telegram plugin and Claude plugins:
- `# - ~/.bun      # Uncomment if using Telegram channel (Bun runtime)`
- `# - ~/.claude    # Uncomment if using Claude Code plugins`

These should be COMMENTED OUT in the base policy.yaml.

Then **create** `templates/right/policy-telegram.yaml` as a copy with these differences:
1. The `telegram_api` network policy entry is UNCOMMENTED
2. `~/.bun` and `~/.claude` read_only entries are UNCOMMENTED
3. A comment at the top noting this is the Telegram-enabled variant

The two files should be nearly identical. The Telegram variant is used when user provides a bot token during `rightclaw init`.
  </action>
  <verify>
    <automated>grep -q "hard_requirement" templates/right/policy.yaml && grep -q "include_workdir" templates/right/policy.yaml && grep -q "api.github.com" templates/right/policy.yaml && grep -q "# telegram_api" templates/right/policy.yaml && test -f templates/right/policy-telegram.yaml && grep -q "api.telegram.org" templates/right/policy-telegram.yaml && grep -c "^  telegram_api:" templates/right/policy-telegram.yaml | grep -q "1" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - templates/right/policy.yaml contains `hard_requirement`
    - templates/right/policy.yaml contains `include_workdir: true`
    - templates/right/policy.yaml contains `api.github.com` in network_policies
    - templates/right/policy.yaml has Telegram section COMMENTED OUT (grep `# telegram_api` matches)
    - templates/right/policy-telegram.yaml exists
    - templates/right/policy-telegram.yaml has `api.telegram.org` UNCOMMENTED
    - Both files contain HOW TO comments for filesystem and network expansion
    - Both files have `version: 1` at top level
  </acceptance_criteria>
  <done>Two policy.yaml variants exist: base (Telegram commented) and telegram (Telegram uncommented), both with hard_requirement Landlock and comprehensive documentation comments</done>
</task>

</tasks>

<verification>
- `cat templates/right/BOOTSTRAP.md` shows conversational onboarding with 4 questions
- `grep -c "HOW TO" templates/right/policy.yaml` returns at least 4 (filesystem + network expansion instructions)
- `diff templates/right/policy.yaml templates/right/policy-telegram.yaml` shows only Telegram-related differences
- All three files are valid (no YAML syntax errors in policy files, markdown renders correctly)
</verification>

<success_criteria>
- BOOTSTRAP.md ready for include_str! embedding with complete onboarding flow
- policy.yaml is a self-documenting OpenShell policy reference with commented Telegram section
- policy-telegram.yaml is the Telegram-enabled variant
- Files match all locked decisions D-01 through D-15
</success_criteria>

<output>
After completion, create `.planning/phases/03-default-agent-and-installation/03-01-SUMMARY.md`
</output>
